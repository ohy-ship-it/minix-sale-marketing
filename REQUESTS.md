# 작업 요청 기록

minix 통합 워크스페이스에 대한 요청과 처리 내용을 순서대로 정리한 문서입니다.
각 항목의 커밋 해시로 실제 변경을 확인할 수 있습니다.

---

## 1. 광고소재 기획 보드 (Notion 이관)

### 1-1. 카드 클릭 편집
> "생성된 카드 클릭 시 수정할 수 있게 해줘"

- 카드 클릭 → 편집 열기 (`4a55b32`)
- **함께 고친 것**: 수정할 때마다 클릭 리스너가 중복 등록되던 문제, 드래그 직후 편집창이 열리던 문제,
  새로고침 때 기본 카드가 두 배로 늘어나던 문제

### 1-2. Notion 스키마로 재구축
> "노션 편집기능 줬거든? 똑같이 구현해" + 공유 시트 링크

- 공개 링크의 내부 API로 컬렉션 스키마와 행 5건을 그대로 읽어 이관 (`e7ad3dc`)
- 속성 10종을 원본과 동일한 이름·타입·옵션·색상으로 구현
  (이름 / 행사명 / 상태 / 매체 / 행사채널 / 담당자 / 행사일자 / 희망전달날짜 / SKU / 세팅)
- 카드 클릭 시 **우측 사이드 피크** 패널, 저장 버튼 없이 입력 즉시 반영
- 상태 변경·드래그로 컬럼 이동, 기간 날짜, 카드 삭제, Esc·배경 클릭 닫기
- **함께 고친 것**: 사용자 입력이 이스케이프되지 않아 제목에 태그를 넣으면 실행되던 문제

### 1-3. 화면 정리
> "이 위에 있는거 없애고, 담당자가 본인 이름 누르면 본인 이름이 있는 카드만 뜰 수 있게"

- 보드 화면에서 상단 헤더 숨김, 담당자 필터 추가 (`fe84a1f`)
- 툴바를 **요청하기** 버튼 하나만 남김 (`df50043`)

### 1-4. 속성 정리
> "카드 속성순서 바꿔줘 / 세팅 체크박스는 없애"

- 상태 → SKU → 행사채널 → 행사명 → 담당자 → 행사일정 → 전달희망일정 순서로 (`9d401c7`)
- 세팅 체크박스 제거
- 이후 카드 표시는 **SKU / 행사채널 / 담당자 / 전달희망일정** 만 (`5db74b0`)
- 최종적으로 **SKU 를 맨 위로, 행사채널은 숨김** (`c20c2c9`)

### 1-5. 요청 완료 버튼
> "요청완료하기 버튼 넣어줘"

- 상세 패널 하단에 sticky 버튼, 제목이 비면 닫히지 않고 안내 (`a83397f`)

### 1-6. 필터
> "필터에 년도도 추가" / "SKU별 필터도 만들어줘"

- 담당자 필터 아래 SKU 필터 (`3da6150`) — 같은 줄은 OR, 줄 사이는 AND
- 주간 목록에 년도 필터 (`20f4f70`)

---

## 2. 매체별 요청사항 칸

> "매체 속성 추가하면 그 아래에 매체별로 요청사항 쓸 수 있는 칸 만들어줘"

- 선택한 매체마다 색상이 다른 입력 칸 생성, 기본 문구 자동 삽입 (`aa07da1`)
- **소재 갯수** 칸 추가 (`c0a7b43`) → 숫자만 받던 것을 자유 입력으로 (`c3066d7`)
- 매체 없이도 쓰는 자유 입력란 (`26d3a33`)
- 칸 순서 드래그 변경 + 매체별 **전달 희망일정** (`d8c72ff`)
- 전달 희망일정을 **캘린더 선택**으로, 모든 날짜에 **요일 표기** (`c5ccc39`)
- 칸 삭제 버튼 (`30c068a`) → 오른쪽 끝으로 정렬 (`a0bf199`)

### 2-1. 집계
> "SKU별로 몇건인지" / "담당자별 소재갯수 더해서"

- 보드 상단에 **SKU 건수** 요약 (`20f4f70`)
- 매체별 소재 갯수를 합산한 **소재 갯수** 카드 속성 (`2e380b8`)
- 행사별 건수 → **담당자별 소재 갯수** 로 교체 (`c20c2c9`)

---

## 3. 주간 소재요청 목록

> "이 보드 주차별로 만들예정이거든? 어떻게 하면 깔끔하게 정리할 수 있을까?"

**판단**: 주차마다 독립 보드를 두는 구조를 선택 (카드에 주차 속성을 두는 방식과 비교 후)

- 주간 목록 → 클릭하면 해당 주차 보드 (`a3d8900`)
- 열 구성: 월 / 주차 / 공유링크 (`d8f5f22`) → 월 / 주차 / 건수 / 수정 / 공유 / 삭제 (`28f510b`)
  → 수정 / 공유 / 삭제 순서로 (`981aaf4`)
- 건수는 **SKU 기준**으로 집계
- 월·주차 필터, 주차는 1~5주차 고정, 추가 시 드롭다운 선택 (`32078e5`, `62b286b`)
- 공유링크는 복사 버튼만 노출

---

## 4. 반응형 · 정렬

> "창 크기 조절했을 때, 맞춰서 보여지게" / "세로정렬 확인하고 제대로맞춰" / "간격이 너무 넓음"

실제 브라우저로 5개 폭(1440·1200·900·700·480px)에서 측정하며 수정 (`20f4f70`, `40c81e1`, `62b286b`)

- `.main` 의 `min-width:800px` 제거 — 창을 줄여도 페이지가 가로로 밀리지 않게
- 폭에 따라 보드 컬럼·사이드바·상세 패널 단계별 대응
- **발견해 고친 문제들**
  - 전역 `td{text-align:center}` 때문에 표의 값이 헤더와 어긋나 있었음
  - 사이드바 메뉴가 좁은 폭에서 한 글자씩 세로로 쪼개지던 문제
  - 열 폭 분배가 특정 열에 몰려 값이 흩어지던 문제

---

## 5. 사이드바

> "콘텐츠 밑에 광고소재 파일명, 광고소재 T&D 추가 / KOL라이브 위에 마케팅팀 드롭다운"

- 콘텐츠 하위에 2개 추가, 마케팅팀 그룹 신설 후 KOL라이브 이동 (`6943519`)

---

## 6. 광고소재 파일명 생성기

> "광고소재 파일명을 만드는 아래 구글시트보고 구현해봐"

공개 링크의 시트 12개 탭을 모두 읽어 명명 규칙을 추출 (`b94c1df`)

| 항목 | 규칙 |
|---|---|
| 최종행사명 | `YYMMDD_상품명_행사채널_행사명` |
| 파일명 | `메시지코드-순번` |

- 메시지 유형 **58종**과 코드, 코드별 **마지막 발번 번호**(benefit 535, urgency 448 …)를 시트에서 그대로 이식
- 이후 요청에 따라 파일명 발번만 남기고 단순화 (`f4d4338`)
- 메시지 유형을 고르면 **즉시 발번** (`90f0f70`)
- 입력 칸: 행사일자(캘린더) / 행사채널 / 상품명 / 행사명 / 매체 / 메시지 유형 (`3ff0368`, `bafbcae`, `6ae1ec4`)
- **(세로)** 처리: 자동 생성 → 요청 시 생성 → 체크박스 토글로 변경 (`e703be9`, `265b354`, `d3d82ba`)
  체크하면 목록의 각 파일명 아래에 `(세로)` 가 붙고, 해제하면 사라짐
  건수·복사·시트 적재에서는 제외
- **전체 지우기**: 입력값 + 발번 목록 모두 비움, 번호는 되돌아가지 않음 (`28bf3c7`, `3469c0a`)
- **매체에서 `마케팅팀` 삭제** — 파일명 화면 목록에서만 뺐습니다.
  UTM 빌더 대응표와 시트 `설정` 탭에는 남아 있습니다(빈 값이라 UTM 을 만들지 않습니다).
- **메시지 유형 검색** — 58종이 길어져 드롭다운 맨 위에 찾기 칸을 넣었습니다.
  **이름과 코드 아무 쪽이나** 걸리면 나오고(띄어쓰기 · 대소문자 무시), 엔터로 첫 결과가 바로 발번됩니다.
  다시 그려도 커서가 검색칸에 남습니다.
- **표로 복사 → 파일명만 복사**: 목록에 보이는 파일명을 한 줄에 하나씩 복사합니다.
  `(세로)` 도 실제 소재 이름이라 함께 복사합니다. 매체 · 최종행사명은 더 이상 따라가지 않습니다.

### 6-1. 메시지 유형 직접 추가
> "직접 작성하기 항목 만들어주고 팝업으로 기재 (한글·특수문자·띄어쓰기 안됨)"

- 처음엔 `window.prompt` 로 구현 (`d851026`) → **화면 안 모달**로 교체 (`8184f83`)
- 메시지 유형명(한글 가능) + 파일명 코드(영문·숫자만) 를 함께 입력받아 등록
- 등록한 유형은 드롭다운 하위 항목으로 남고, 다른 유형처럼 번호가 올라감
- 삭제 버튼을 **드롭다운 항목 옆**에 넣기 위해 `<select>` 를 직접 만든 드롭다운으로 교체 (`90ff1bb`, `c6ece7f`)

---

## 7. 구글시트 적재

> "최종완료 버튼 누르면 아래 구글시트에 적재해줄 수 있어?"

브라우저에서 시트에 직접 쓰려면 인증이 필요해, 시트 쪽에 Apps Script 웹 앱을 두는 방식으로 연결 (`265b354`)

- [Code.gs](Code.gs) · [google-apps-script.md](google-apps-script.md) 로 설정 방법 제공
- 배포 주소를 코드에 고정, 실제 적재까지 확인 (`f31cf48`, `1393ba4`)
- Apps Script 는 **고칠 때마다 새로 배포**해야 하고, 그때 주소가 바뀝니다.
  주소는 `app.js` 의 `SHEET_ENDPOINT` 한 곳에만 두어 거기만 갈아 줍니다 (`af9f805`)
- 적재 항목: **파일명 / 매체 / 메시지 유형 / 최종행사명 / 발번시각** (`e1fef32`)
  앱이 열 정의를 함께 보내므로 앞으로 열이 늘어도 재배포 불필요
- 버튼을 **적재확인** 으로 바꾸고 누르면 시트를 새 탭으로 열도록

**중간에 걸렸던 것**
- 첫 배포가 도메인 제한이라 로그인 페이지로 넘어가 요청이 막힘 → 액세스 권한을 "모든 사용자"로 재배포
- 문서(.md)를 통째로 붙여넣어 `SyntaxError` → 코드만 담은 `Code.gs` 파일 분리

---

## 8. 광고소재 검수

> "https://ad-creative-checker.onrender.com/ 이 사이트 추가해줄 수 있어?"

- 임베드 차단 헤더가 없어 화면 안에 iframe 으로 삽입 (`f04c855`)
- 메뉴에 들어갈 때만 로딩(무료 플랜이라 첫 로딩이 느림), 새로고침·새 탭 열기 버튼 제공
- **고친 것**: `display:flex` 가 `hidden` 속성을 덮어써서 다른 화면에도 보이던 문제 (`2327c81`)

### 8-1. 검수 사이트와 주고받기

검수할 때마다 소재명을 다시 타이핑하고, 결과는 복사해 시트에 붙여 넣던 두 단계를 없애는 연동입니다.
같은 창 안의 iframe 이라 서버·CORS 없이 `postMessage` 로 주고받습니다 (`54234f7`).

| 방향 | 주고받는 것 |
|---|---|
| 워크스페이스 → 검수 | `ping` (연결 확인) · `prefill` (소재명 · 행사일자 · 상품명 · 행사채널 · 매체 · 파일명) |
| 검수 → 워크스페이스 | `ready` (응답) · `result` (채널 · 소재명 · 배경색 · 표 문자열) |

- 소재 정보는 **광고소재 파일명 화면에 저장된 것을 그대로 물려받습니다**(`minix-filename-tool-v3`).
  `[검수로 보내기]` 를 누르면 넘기고, 연동 전에도 쓸 수 있게 클립보드에도 같이 넣습니다.
- 화면 위 배지가 **연동 대기 중 → 연동됨** 으로 바뀌면 저쪽 수신 코드가 붙은 것입니다.
  iframe 이 뜨면 `ping` 을 0.8초 간격으로 **다섯 번만** 보내고 멈춥니다(응답이 없어도 화면은 그대로 동작).
- 받은 표는 화면 아래에 그대로 펼쳐 주고 **표 복사** 버튼을 답니다.
- 보낼 때도 받을 때도 origin(`ad-creative-checker.onrender.com`)을 확인합니다.

**아직 반쪽입니다** — 워크스페이스 쪽만 구현돼 있습니다. 검수 사이트에 수신 코드를 넣어야 이어집니다.
넣을 코드와 매체 → 검수 채널 대응표는 [INTEGRATION.md](INTEGRATION.md) 에 그대로 적어 두었습니다.
연동이 안 된 상태에서도 예전처럼 **복사 → 시트 붙여넣기** 로 쓸 수 있습니다.
받은 표를 시트에 적재하는 것도 아직입니다(지금은 복사까지).

---

## 9. 광고자동 세팅 · 메타 광고 세팅

> "퍼포먼스 드롭다운 밑으로 광고자동 세팅 추가하고 아래 경로에 있는 프로그램 메뉴로 구현시켜"
> `C:\Users\o_hy\Downloads\공유_NAS수정판\공유_NAS수정판`

사내 배포용 HTA(`meta-ad-setup.hta`) 를 워크스페이스 메뉴로 옮겼습니다.
퍼포먼스 아래에 **광고자동 세팅** 그룹을 만들고 **메타 광고 세팅** 화면을 넣었습니다.

**역할을 나눈 이유**

원본은 ActiveX 로 `.env` 를 읽고, NAS 를 마운트하고, PowerShell 을 띄웁니다.
브라우저는 셋 다 할 수 없어 이렇게 갈랐습니다.

| | 원본 HTA | 이 화면 |
|---|---|---|
| 폼 · 시트 조회 · 검증 | HTA | **브라우저** |
| NAS 접근 · 소재 업로드 · 메타 API | PowerShell | **내려받은 `.ps1`** (원본과 같은 코드) |
| 토큰 · NAS 비밀번호 | `.env` 직접 읽기 | **`.ps1` 이 `.env` 직접 읽기** (브라우저에 저장 안 함) |

PowerShell 본문 249줄은 HTA 에서 기계적으로 추출해 그대로 씁니다. 손으로 옮겨 적지 않았습니다.

**그대로 옮긴 것**

- 광고 계정 8개 · 목적 · URL 유형(URL/LINK(GA)/NT/쇼핑라이브) 선택
- `utm-builder` 시트 조회 → 표에서 행 선택 → 캠페인명·광고그룹명·랜딩 URL 자동 입력
- 예산 한글 읽기(`1,000,000` → `백만원`), 목적별 최적화·과금 기준, 성별·연령 타겟
- 소재 파일명의 메시지코드로 광고명을 되찾는 `$adMapping`, `(세로)` 소재 게재위치 분리
- 광고는 항상 `PAUSED` 로 생성

**바꾼 것**

- 날짜: 년/월/일/시/분 드롭다운 10개 → `datetime-local` 2개
- 임시저장 버튼 → 입력 즉시 localStorage 자동 저장
- 실행이 다른 곳에서 일어나므로, 원본이 **실행 도중에야** 냈던 오류를 만들기 전에 잡습니다
  (계정 미선택, 예산 0원, 총예산인데 종료일 없음, 시트 조회 안 함, T&D 시트에 프로모션 없음 등)
- 시트 조회 시 `[DA] 메타` 의 제목·문구를 **미리 보여줍니다**
  ("Step 1 제목/문구가 비어서 들어감" 은 원본 README 의 대표 오류였습니다)
- 시트 데이터 시작 행을 3행 → 2행으로 (원본은 첫 데이터 행을 조용히 건너뛰었습니다)

### 9-1. NAS 인증부 보강 (`자동_셋팅_도구_오류_진단.pdf` 대조)

진단서가 지목한 6가지 중 5가지는 이미 수정판에 반영돼 있었습니다.
공유 폴더 루트 연결 · 기존 세션 검사 · 종료코드 검사 · UTF-8 BOM · 중복 광고 방지.
(BOM 은 진단서가 권한 `'\uFEFF' + psScript` 와 같은 처방을, 파서 오류를 보고 독립적으로 넣었습니다.)

**남아 있던 것 하나 — `net use` 대신 `New-PSDrive`**

`net use $shareRoot "/user:$nasU" $nasP` 는 **비밀번호가 프로세스 명령행에 남습니다.**
자격 증명 객체를 쓰도록 바꿨습니다. 이 프로세스에서만 살아 있어 사용자의 기존 매핑도 건드리지 않습니다.

```powershell
$nasCred = [pscredential]::new($nasU, (ConvertTo-SecureString $nasP -AsPlainText -Force))
New-PSDrive -Name NASSETUP -PSProvider FileSystem -Root $shareRoot -Credential $nasCred -Scope Script
```

**바꾸다가 발견한 것 — 진단서도 수정판도 놓친 구멍**

바꾼 코드를 실제로 돌려보니 **가드를 통과하고도 1219 가 났습니다.** 원인은 이렇습니다.

- 가드는 `Get-SmbConnection`(활성 세션)만 봅니다
- 사내망 밖이거나 유휴 상태면 **세션은 사라지지만 `Z:` 같은 영구 매핑은 남습니다**
- 그래서 가드는 "연결 없음"으로 판단 → 재인증 → **1219**

이 PC 상태가 정확히 그랬습니다. `Get-SmbConnection` 은 비었는데
`net use` 에는 `Z: → \\192.168.1.100\앳홈_공유폴더` 가 남아 있었습니다.

`Get-SmbMapping` 검사를 더해, 매핑이 있으면 **인증을 건너뛰도록** 고쳤습니다.
1219 메시지가 숫자 없이 문장으로만 오는 경우도 있어 영문·한글 문구까지 함께 봅니다.

| 상태 | 동작 |
|---|---|
| 다른 계정 세션 있음 | 중단 (기존과 동일) |
| 같은 계정 세션 있음 | 재사용 |
| **세션 없고 매핑만 있음** | **인증 건너뜀** ← 새로 추가 |
| 아무것도 없음 | `New-PSDrive` 로 인증 |

---

**중간에 걸렸던 것**

- 내려받은 `.ps1` 에 **BOM 이 없으면** PowerShell 5.1 이 ANSI 로 읽어 한글이 깨집니다.
  실제로 파서를 돌려보니 한글 바이트가 따옴표로 오인돼 구문 오류 17건이 났고, BOM 을 붙이자 통과했습니다.
- 메뉴 해시를 섹션 id 와 같은 `#ad-setup` 으로 뒀더니 브라우저가 그 요소로 스크롤 →
  해시만 `#meta-ad-setup` 으로 분리
- `.content-tabs` 를 숨기는 규칙에 새 화면을 빠뜨려 대시보드 탭이 남아 보임 (8번과 같은 유형)

---

## 10. UTM 자동생성

> "utm빌더 워크스페이스에 만들려고 하는데, utm-builder 탭 참고해서
> 지금 광고소재 파일명 적재되는 구글시트에서, 파일명 들어가면 utm 자동생성 될 수 있게. 랜딩링크랑 목적은 공란으로 두고"

참고 시트의 `utm-builder` 탭과 그 탭이 `VLOOKUP` 으로 물고 있는 `index` 탭을 읽어
규칙을 파일명 적재 시트 쪽([Code.gs](Code.gs))으로 옮겼습니다.

**만드는 곳을 앱이 아니라 시트로 잡은 이유**

랜딩링크와 목적은 파일명만으로 알 수 없어 사람이 나중에 채웁니다.
앱이 값을 계산해 보내면 그때 이미 고정돼, 나중에 목적을 채워도 캠페인명이 따라오지 않습니다.
그래서 **값이 아니라 수식**을 그 줄에 심어 뒀습니다. 두 칸을 채우는 순간 나머지가 따라 만들어집니다.

| 항목 | 규칙 | 비고 |
|---|---|---|
| `utm_source` · `utm_medium` | 매체 대응표 | index 탭 |
| `utm_campaign` | 목적(영문) | 목적을 채우면 생성 |
| `utm_content` | `YYYYMMDD_제품_파일명` | 메시지코드는 번호까지가 한 덩어리 |
| `utm_term` | 검색매체면 `{keyword}` | utm-builder 와 동일 |
| `LINK(GA)` | 랜딩링크 + utm 파라미터 | 랜딩링크를 채우면 생성 |
| `NT` | 랜딩링크 + `?nt_source`(소스_미디엄) · `nt_medium`(콘텐츠) | 네이버 리포트에서 소재를 미디엄으로 봄 |
| `FM(쇼핑라이브)` | 랜딩링크 + `?fm` · `sn` · `ea` | 랜딩링크가 비면 `?` 로 시작하는 파라미터만 |

- **대응표는 코드가 아니라 시트에** — 같은 문서에 `설정` 탭을 만들어 매체 · 목적 · 상품명 대응표를 넣었습니다.
  매체가 늘어도 재배포 없이 시트에서 고칩니다.
- **앱 적재 · 직접 입력 둘 다** — `doPost` 와 `onEdit` 가 같은 함수를 씁니다.
  이미 쌓인 줄은 시트 상단 **UTM → 전체 행 다시 계산** 으로 채웁니다.
- **손으로 고친 UTM 은 되돌리지 않습니다** — `onEdit` 는 앞쪽(입력) 칸을 건드렸을 때만 다시 씁니다.

### 10-1. FM(쇼핑라이브) 링크

> "FM 쇼핑라이브 링크도 만들어줘"

utm-builder 시트의 FM 열 수식 `P&$AC$3&AC&$AD$3&AD&$AE$3&AA`
= `url & "&fm=" & source & "&sn=" & 지면 & "&ea=" & 광고명` 을 옮겼습니다.

**처음 읽었을 때 열을 한 칸 잘못 봤습니다.** xlsx 파싱에서 빈 셀(`<c r="R2"/>`)을 건너뛰다
뒤 셀을 삼켜 `NT` 가 R 열로 보였습니다. XML 파서로 다시 읽어 `Q=LINK(GA) · S=NT · T=FM` 으로 바로잡았습니다.
(app.js 의 시트 조회 코드는 처음부터 맞게 `r[19]` 를 쇼핑라이브로 읽고 있었습니다.)

### 10-2. 열 차례를 utm-builder 에 맞춤

> "하위메뉴 광고파일명에서 가져올 수 있는 정보로 열/채우는 주체를 utm빌더에 맞춰서 다시 짜주고,
> 지금 내가 수동으로 수정한 값을 최종으로 해놔. 쇼핑라이브 행 지우고, FM을 앞으로 당겨"

시트에서 직접 고치신 값을 읽어(`xlsx` 내보내기) 그대로 코드에 반영했습니다.

| 고치신 것 | 반영 |
|---|---|
| 설정 매체표 `gfa·feed` / `kakao·bizboard` / `google·demandgen` … | `DEFAULT_MEDIA` 기본값 교체 |
| `utm_medium` 에 지면을 직접 적음 | `sn(지면)` 칸을 없애고 쇼핑라이브 `sn` 도 `utm_medium` 을 쓰게 |
| `utm_campaign` 수식을 `=G2`(목적) 로 바꿈 | `utm_source_목적` → **목적만** |
| 목적 칸에 영문(`purchase`) 입력 | 드롭다운은 목적(영문) 목록, 한글을 넣으면 영문으로 옮김 |

- **쇼핑라이브링크 열 삭제** — `OBSOLETE_HEADERS`. 값이 남아 있으면 지우지 않습니다.
- **열 차례 정렬** — `orderColumns_` 가 `moveColumns` 로 왼쪽으로만 끌어옵니다.
  결과는 utm-builder 와 같은 `url → LINK(GA) · NT · FM → utm_*`.
- **NT · FM 도 랜딩링크를 앞에 붙입니다** — 처음엔 주소가 다를까 봐 파라미터만 만들었는데,
  utm-builder 시트도 url 한 칸으로 세 링크를 만들고 있어 같은 방식으로 맞췄습니다.
  랜딩링크가 비면 `?` 로 시작하는 파라미터만 남아 그대로 복사해 쓸 수 있습니다.

### 10-3. utm_content 에서 메시지코드 번호가 빠져 있던 것

> "ga 링크에서 베네핏뒤에 숫자 안들어간거 확인"

파일명 `benefit-537` 을 **메시지코드(benefit) + 순번(537)** 으로 쪼개
`537_20260829_flender-mini_benefit` 을 만들고 있었습니다. 앞뒤로 흩어져 파일명을 되짚기 어려웠습니다.

utm-builder 의 메시지 코드는 `celebrity-51` · `painpoint-3` 처럼 **번호까지가 한 덩어리**입니다.
우리 파일명도 마찬가지라 쪼개지 않고 통째로 뒤에 붙입니다.

```
전  537_20260829_flender-mini_benefit
후  20260829_flender-mini_benefit-537
```

앞에 있던 순번은 파일명 안에 이미 들어 있어 뺐습니다.

> `(세로)` 가 붙은 파일명은 처음엔 표시만 떼고 UTM 을 만들었는데,
> **가로와 같은 소재라 UTM 도 하나면 됩니다.** 그 줄은 아예 만들지 않도록 바꿨습니다(`VERTICAL_MARK`).
> 앱도 적재에서 세로를 빼므로 손으로 적어 넣었을 때만 걸립니다.

**남겨 둔 것**

- utm-builder 의 광고명은 `no_날짜_제품_메시지코드_소재유형_담당자` 인데,
  파일명 시트에 **소재유형 · 담당자가 없어** 뒤 두 토막이 빠집니다.
- **연령 · 타겟팅 · 프로모션**이 없어 utm-builder 의 **그룹** 열은 만들 수 없습니다.

**확인 방식**

시트에 쓸 수 없어 두 가지를 만들어 돌렸습니다.
수식이 실제로 어떤 문자열을 내놓는지 보는 **작은 수식 평가기**(IF · IFERROR · VLOOKUP · TEXTJOIN · SPLIT · REGEXEXTRACT …),
그리고 `doPost` · `onEdit` · `fillAllUtm` 를 태워 보는 **가짜 SpreadsheetApp** 입니다.
공란 · 대응표에 없는 매체 · 이미 `?` 가 붙은 랜딩링크 · 파일명 삭제 등 12가지 경우를 확인했고,
가짜 시트는 **지금 실제 시트 상태(옛 열 차례 + 쇼핑라이브링크)** 에서 시작해 열 삭제 · 차례 정렬까지 확인했습니다.

---

## 11. UTM 빌더 화면 (광고자동 세팅)

> "지금까지 했던 규칙들 토대로 워크스페이스 광고자동세팅 하위메뉴로 UTM 빌더 만들어주고
> utm 수동으로 만들 수 있는 메뉴 구현해. 수동으로 만든 utm 만 엑셀 다운로드할 수 있는 기능 추가까지"

`광고자동 세팅` 아래 **UTM 빌더** 를 넣었습니다. 시트를 거치지 않고 그 자리에서 UTM 을 만듭니다.

- 입력: 파일명 · 매체 · 목적 · 행사일자 · 상품명 · 랜딩링크
- 출력: **LINK(자사몰) · NT(네이버) · FM(라이브)** 를 실시간 미리보기 (항목마다 복사).
  URL 유형 버튼과 미리보기 이름을 같은 말로 맞췄습니다. utm_* 낱개 값은 링크 안에 이미 들어 있어 뺐습니다.
- **파일명 칸은 `광고소재 파일명` 발번 목록에서 고릅니다.** 고르면 그때 만든
  매체 · 행사일자 · 상품명이 함께 채워집니다 (세로형은 목록에서 뺍니다).
- 목록(**UTM 일괄다운로드**)에 담은 것만 CSV 로 받습니다.
  **미리보기에 뜬 것 = 고른 URL 유형 하나**만 링크 열로 나갑니다. 유형을 바꾸면 받는 내용도 바뀝니다.

**시트와 같은 규칙인지**

규칙이 두 곳(Code.gs 수식 · app.js)에 있으므로, **같은 값을 내놓는지 자동으로 맞대어 봤습니다.**
시트 수식을 계산하는 평가기와 실제 화면(jsdom)에 같은 입력을 넣어 8개 항목을 비교합니다.

| 맞대어 본 경우 | 결과 |
|---|---|
| GFA-피드 · 자사몰 / 랜딩링크 없음 / 이미 `?` 가 붙은 링크 / 목적 공란 / 쇼핑라이브 링크 | 5가지 40개 항목 모두 일치 |

- 화면 자체도 jsdom 으로 29가지를 확인했습니다(메뉴 전환 · 자동 채움 · 미리보기 · 추가 · 삭제 ·
  CSV 내용과 BOM · 대응표에 없는 매체는 추가 막기).
- 기존 메뉴 7개를 순회해 오류가 없는 것도 확인했습니다.

**엑셀은 CSV 로 냅니다.** 브라우저만으로 xlsx 를 만들려면 외부 라이브러리를 받아와야 하는데,
UTF-8 BOM 을 붙인 CSV 는 엑셀이 그대로 열고 한글도 깨지지 않습니다.

**대응표가 두 벌인 것**

매체 · 목적 · 상품명 대응표가 시트의 `설정` 탭과 화면 양쪽에 있습니다.
시트에서 대응표를 고치면 화면 쪽(`app.js` 의 `MEDIA` · `PURPOSES` · `PRODUCTS`)도 같이 고쳐야 합니다.
화면이 시트를 읽게 하면 한 벌로 줄일 수 있지만, 시트 조회가 실패하면 화면이 멈추므로 지금은 두 벌로 뒀습니다.

### 11-1. 인플루언서 · 여러 건 한꺼번에 · 시트 적재

> "매체에 인플루언서 추가하고, 시트에 매칭할 수 있는거 추가해주고
> 파일명 한꺼번에 등록할 수 있는 형태로 변경. 추가로 만든 utm, 파일명 시트에 업로드할수 있게
> 최종완료버튼 누르면 생성/시트에 업로드"

| 요청 | 한 일 |
|---|---|
| 매체에 인플루언서 | `인플루언서 → influencer · affiliate` (index 탭 기준). 파일명 생성기 매체 목록에도 추가 |
| 시트에 매칭 | **설정 탭에 빠진 줄만 자동으로 덧붙입니다.** 고쳐 둔 값은 건드리지 않습니다 |
| 파일명 한꺼번에 | 한 줄에 하나씩 적는 칸 + 발번 목록 칩. 여러 건이면 한 번에 그만큼 만듭니다 |
| 최종완료 | 입력한 것을 목록에 담고, 아직 안 올린 것을 파일명 시트에 적재 |

**URL 유형 버튼**을 메타 광고 세팅과 같은 모양으로 넣었습니다(URL · LINK(GA) · NT · 쇼핑라이브).
고른 유형이 목록 표의 열 이름 · 값 · 행 복사에 그대로 쓰이고, 미리보기에서도 그 줄이 표시됩니다. 기본값은 LINK(GA).

**적재할 때 UTM 값은 보내지 않습니다.** 파일명 · 매체 · 최종행사명 · 랜딩링크 · 목적만 보내고,
UTM 은 시트가 자기 수식으로 다시 만듭니다. 값을 보내면 나중에 시트에서 고쳐도 따라오지 않기 때문입니다.

**그러려면 적재가 열 이름을 봐야 했습니다.** 기존 `doPost` 는 보낸 차례대로 A열부터 채워서,
랜딩링크(6번째 열)를 세 번째로 보내면 엉뚱한 칸에 들어갔습니다.
헤더에서 이름을 찾아 그 자리에 넣도록 바꿨습니다.

- 배포 주소가 두 화면에 필요해져 `SHEET_URL` · `SHEET_ENDPOINT` 를 위로 올렸습니다. 주소는 한 곳에서만 고칩니다.
- 파일명을 **칩으로 고르면** 매체 · 행사일자 · 상품명을 덮어쓰고, **직접 칠 때는** 매체가 비어 있을 때만 가져옵니다.
  여러 건을 한 매체로 만들 때 매번 덮어쓰면 방해가 되기 때문입니다.

---

## 12. 퍼포먼스일정 (마케팅팀)

> "마케팅팀 드롭다운에서 하위메뉴로 CRM 일정 추가하고 광고소재 기획과 동일하게 아래 노션이랑 똑같이 구현"
> → "형식을 만들라는거지 내용은 안가져와도됨"
> → "메뉴이름 브랜드검색 일정으로 바꿔주고, 26년9월꺼만 내용가져와"
> → "위에 아까 만들어줬던 필터 넣어줘"
> → "< 오늘 > 이거 년월로 바꿔주고 / 전 sku 퍼포 스케줄링 제목 삭제 / 하위메뉴명 퍼포먼스일정으로"

노션 **[전 SKU] 퍼포 스케줄표** 를 공개 링크의 내부 API 로 읽어
**속성 · 옵션 · 색상 · 뷰 종류**를 그대로 옮겼습니다.
일정은 전체 141건 중 **26년 9월 9건만** 넣었습니다 (이름 · 기간 · SKU · 판매채널 · 매체 · 세팅완료 그대로).

| 속성 | 형식 | 옵션 |
|---|---|---|
| 이름 | 제목 | |
| SKU | 다중 선택 | 더플렌더 mini · MAX · PRO · 에어드라이 · 더시프트 |
| 날짜 | 날짜(기간) | |
| 매체 | 다중 선택 | 브검 · 페이드 · CRM |
| 판매채널 | 다중 선택 | 오늘의집 · CJ · 네이버 … 18종 |
| 세팅완료 | 체크박스 | |

**뷰는 캘린더입니다.** 원본이 `캘린더 보기` 하나뿐이라 그대로 맞췄습니다.

### 겉모습을 노션과 맞춘 것 (실제 화면 대조)

> "보이는 비주얼 노션이랑 똑같이해" + 노션 화면 스크린샷

헤드리스 크롬으로 노션 화면을 찍으려 했지만 **클라우드플레어 봇 검사에 막혀**,
받은 스크린샷과 하나씩 대조해 고쳤습니다.

| | 처음 | 노션에 맞춘 뒤 |
|---|---|---|
| 날짜 숫자 | 왼쪽 · 파란 원(오늘) | **오른쪽 · 빨간 원**, 1일엔 `9월 1일` |
| 일정 | 매체 색 막대 한 줄 | **흰 카드 + 얇은 테두리**, 안에 SKU · 날짜 · 매체 · 세팅완료 |
| 날짜 표기 | 없음 | `2026년 9월 2일 → 2026년 9월 4일` |
| 세팅완료 | 취소선만 | **체크박스 모양** (완료 시 파란 체크) |
| 요일 머리글 | 왼쪽 정렬 | **가운데 정렬**, 아래 실선만 |
| 파란 버튼 | 있었음 | **뺐습니다** (노션 화면에 없음) |
| 필터 | 뺐다가 | **다시 넣었습니다** (달력 위, 광고소재 기획과 같은 규칙) |
| 일정 추가 | 파란 `일정 추가` 버튼 | **칸에 마우스 올리면 `+`** (노션과 동일) |

카드 안 속성은 노션 뷰 설정(`calendar_properties`)을 그대로 따릅니다 —
**판매채널은 그 뷰에서 숨김**이라 카드에 넣지 않았습니다.

### 카드를 끌어 일정 조절

> "카드 땡기거나 줄이면 일정조율되게 해줘"

- **카드를 끌면** 기간이 통째로 옮겨집니다 (하루짜리는 하루짜리로).
- **양 끝을 잡으면** 시작일 · 종료일만 바뀝니다. 마우스를 올리면 잡는 손잡이가 보입니다.
- 놓는 순간 저장되고, 상세 패널이 열려 있으면 날짜도 함께 갱신됩니다.

날짜는 **칸 단위로만** 바뀝니다. 포인터가 있는 주(週)의 가로 위치로 요일을 계산해,
주 경계를 넘어가도 그대로 이어집니다.

**끌어 놓은 직후의 click 을 걸러내는 방법** — 처음엔 `setTimeout` 으로 표시를 지웠는데,
그 타이머가 브라우저의 click 보다 **먼저** 돌면 상세가 열려 버립니다.
지금은 **다음 mousedown 에서 지웁니다.** 진짜 클릭은 항상 mousedown 이 앞서므로 새는 경우가 없습니다.

- 기간 일정은 **막대로 이어 그립니다.** 한 주 안에서 겹치지 않게 줄(lane)을 나눠 놓고,
  주 경계를 넘으면 다음 줄에서 이어집니다. 막대 색은 매체를 따릅니다(CRM=파랑).
- **날짜 칸을 누르면** 그 날짜로 일정이 만들어지고 바로 사이드 피크가 열립니다.
- 편집은 광고소재 기획과 같은 방식입니다 — 같은 `peek-*` 마크업과 칩 색을 씁니다.
- 필터도 같은 규칙(같은 줄 OR, 줄 사이 AND). 매체 · SKU 두 줄입니다.
- **일정이 있는 달로 엽니다.** 이번 달이 비어 있으면 가장 가까운 달로 넘어갑니다.
- 달 이동은 `‹ 오늘 ›` 대신 **년 · 월 고르개**입니다. 년도 목록은 일정이 있는 해 앞뒤로 한 해씩.
- 메뉴 이름은 CRM 일정 → 브랜드검색 일정 → **퍼포먼스일정** 으로 두 번 바뀌었습니다.
  화면 id 와 저장키(`brand-schedule`)는 그대로 뒀습니다. 바꾸면 이미 넣어 둔 일정이 사라집니다.
- 시드는 **처음 한 번만** 저장합니다. 지운 일정이 새로고침 때 되살아나지 않습니다.

**노션 스키마를 읽은 방법** — 공개 페이지의 `loadPageChunk` 로 컬렉션 id 를 찾고,
`queryCollection` 으로 스키마(속성 이름 · 타입 · 옵션 색)를 그대로 가져왔습니다.

**확인** — jsdom 으로 39가지(메뉴 위치 · 옵션 목록 · 9월 9건이 그대로 들어왔는지 · 달 이동 ·
날짜 칸 클릭 · 제목/매체/SKU/체크박스 편집 · 기간 막대 · 필터 · 삭제)를 확인했습니다.

**테스트를 짜면서 걸린 것 두 가지** — 둘 다 코드가 아니라 테스트가 틀렸습니다.
10월 달력에는 9월 말 일정이 걸쳐 보이는 게 맞고(노션도 같습니다),
`toISOString()` 은 UTC 라 한국 시간 기준 날짜가 하루 밀립니다.

---

## 13. 적재시트에 광고자동세팅용 이름 추가

> "적재시트 기준으로 메타광고자동세팅 하려고 하는데, 광고자동세팅 규칙에 맞춰서
> 적재시트 내용추가해줘. 캠페인명, 광고그룹명 같은거"

메타 광고 세팅이 지금은 `utm-builder` 시트를 조회합니다. 그 시트가 주는 값은
**캠페인명 · 광고그룹명 · 광고명 · 메시지코드 · URL 4종** 입니다.
적재시트가 같은 값을 내놓도록 열을 더했습니다.

| 열 | 채우는 주체 | 규칙 |
|---|---|---|
| 연령 · 타겟팅 | **사람** | 광고그룹명에만 쓰입니다 |
| 캠페인명 | 수식 | `[제품]utm_source_목적` |
| 광고그룹명 | 수식 | `[행사명]연령_타겟팅_매출채널` |
| 광고명 | 수식 | `utm_content` 와 같은 값 |

```
최종행사명  260829_더플렌더mini_CJ_음쓰해방위크
        ↓
캠페인명    [더플렌더mini]gfa_purchase
광고그룹명   [음쓰해방위크]25-54_interestTarget_cj
```

- **최종행사명 한 칸에서 세 토막을 꺼냅니다** — 상품명 → 캠페인명, 행사채널 → 매출채널, 행사명 → 광고그룹명 앞머리.
- **매출채널 대응표**를 설정 탭 `K · L` 에 새로 만들었습니다(자사몰 → officialWebsite, CJ → cj …).
  index 탭에는 `officialWebsite` · `naver` 두 개뿐이라 나머지는 이름을 맞춰 넣었습니다. 쓰시는 표기로 고쳐 주세요.
- 연령 · 타겟팅이 비어 있으면 그 토막만 빠집니다 (`[가을맞이]officialWebsite`).

### 13-1. 맨 앞에 행사명 칸

> "적재시트 맨 앞에 행사명 수기로 추가할 수 있겠금 만들어줘"

`A열`에 **행사명** 을 넣었습니다(`FRONT_HEADERS`). 이미 열이 있는 시트에는 1열 자리에 끼워 넣고,
다른 자리에 있으면 앞으로 당겨 옵니다.

**적어 두면 광고그룹명이 그 이름을 씁니다.** 비워 두면 예전처럼 최종행사명의 행사명 토막을 씁니다.

```
행사명 칸 "추석 빅세일"  →  [추석 빅세일]25-54_interestTarget_cj
행사명 칸 비어 있음      →  [음쓰해방위크]25-54_interestTarget_cj
```

적재는 **열 이름을 보고 자리를 찾으므로**, 앞에 열이 하나 늘어도 앱은 그대로입니다.

### 13-2. 합친 이름 대신 원래 값으로

> "시트 최종행사명에 상품명만 넣는걸로 수정하고 열이름도 상품명으로 / 메시지유형은 없어도 될것 같은데?"

`최종행사명`(`YYMMDD_상품명_행사채널_행사명`) 한 칸을 **행사일자 · 상품명 · 행사채널** 세 칸으로 풀었습니다.
`메시지 유형` 은 더 보내지 않습니다.

**왜 세 칸이 됐나** — 그 한 칸에서 날짜(utm_content) · 상품(캠페인명) · 채널(매출채널)을 꺼내 쓰고 있었습니다.
상품명만 남기면 나머지 둘이 조용히 사라지므로, 원래 값을 각각 받게 했습니다.
문자열을 쪼개지 않으니 수식도 단순해지고, 시트에서 한 칸만 고쳐도 결과가 따라옵니다.

- 행사일자는 **날짜로 들어오든 글자로 들어오든** `YYYYMMDD` 로 만듭니다(`ISNUMBER` → `TEXT` / `SUBSTITUTE`).
- 옛 열(`메시지 유형` · `최종행사명`)은 **비어 있으면 지우고, 값이 남아 있으면 맨 오른쪽으로 밀어** 둡니다.
- 열 배치 방식을 바꿨습니다. 예전에는 UTM 열만 왼쪽으로 당겼는데, 앞쪽에 열이 늘자 차례가 꼬였습니다.
  지금은 **정해진 차례 전체**를 앞에서부터 당겨 옵니다. 목록에 없는 열은 자연히 오른쪽 끝으로 갑니다.

### 13-3. 광고그룹명이 비던 것

> "캠페인명에 광고그룹명이 나오고, 광고그룹명은 비어있음"

시트를 열어 보니 그 줄은 **UTM 빌더로 적재한 줄**이었습니다.
그 화면이 행사채널 · 행사명을 안 보내서 광고그룹명 재료(`[행사명]연령_타겟팅_매출채널`)가 하나도 없었습니다.
캠페인명에 있던 `[더플렌더mini]facebook_purchase` 는 규칙대로 나온 **캠페인명**이 맞습니다.

- **UTM 빌더에 행사채널 · 행사명 칸을 넣었습니다.** 파일명을 붙여넣어 물려받을 때도 함께 따라옵니다.
- **열 차례를 화면과 무관하게 고정했습니다.** 예전에는 이번에 안 보낸 열이 오른쪽 끝으로 밀렸습니다
  (그래서 행사채널이 utm_term 뒤에 가 있었습니다). 지금은 기준 차례를 늘 먼저 깔고 시작합니다.

### 13-4. 빌더 시트 실제 값과 대조

> "빌더 캠페인, 그룹, 광고명 다시 확인" + 시트 화면

제가 옮긴 규칙은 utm-builder **초기 행**(4행)의 것이었습니다. 최근 행은 규칙이 달라져 있었습니다.

| | 옮겼던 것 | 실제 빌더 |
|---|---|---|
| 캠페인명 | `[제품]소스_목적` | **`소스_제품 정식명_목적`** — `facebook_미닉스 더 플렌더(mini)_purchase` |
| 광고그룹명 | 빈 칸은 건너뜀 | **빈 칸도 자리를 지킴** — `[naver-260829]_none_naver` |
| 광고명 | 날짜_제품_파일명 | **뒤에 소재유형 · 담당자** — `…_celebrity-77_image_ljm` |

- 설정 상품표에 **제품 정식명**(index 탭 J열) 열을 더했습니다. 이미 있는 줄의 빈 칸도 채워 넣습니다.
- **소재유형 · 담당자** 를 사람이 채우는 칸으로 추가했습니다.

빌더에 `influencer-ig` 로 찍힌 줄이 있어 **인플루언서를 인스타 · 유튜브로 나눴습니다**
(`influencer-ig` · `influencer-youtube`). 옛 줄이 깨지지 않게 설정 표에는 `인플루언서` 도 남겨 뒀습니다.

**아직 못 맞춘 것** — 빌더 광고명 앞의 일련번호(`6581_`)는 그 시트의 행 번호라 옮길 수 없습니다.
우리 쪽은 파일명(`celebrity-77`)이 그 역할을 합니다.

### 13-5. 파트별 탭으로 적재

> "utm 빌더 <매체> 위에 파트 항목 추가 / 세일즈마케팅 · 더플렌더_파트 · 생활가전_파트
> 적재시트 탭이랑 매칭해서 그 탭에 적재"

- UTM 빌더 맨 앞에 **파트** 칸을 넣고, 만든 항목마다 파트를 담아 둡니다.
- **최종완료** 하면 파트별로 나눠 보냅니다(파트 3개면 요청 3번). 목록과 엑셀에도 파트 열이 있습니다.
- 시트는 `payload.sheet` 이름으로 탭을 고릅니다. 없으면 만듭니다.
  `onEdit` 과 **전체 행 다시 계산** 도 세 탭을 모두 돕니다. 설정 탭은 건드리지 않습니다.
- 파일명 화면 적재는 파트를 고르지 않아 **세일즈마케팅** 탭으로 갑니다.

### 13-6. 안 쓰는 열 정리

> "시트에 메타광고세팅이나 빌더 만들때 필요하지 않는 항목은 삭제. 타겟팅은 냅두고"

`utm_source` · `utm_medium` · `utm_campaign` · `utm_content` · `utm_term` **다섯 열을 뺐습니다.**
링크 세 개 안에 이미 들어 있는 값이고, `utm_content` 는 `광고명` 과 같은 값이었습니다.
수식은 그 자리에서 계산하도록 바꿨습니다(열 참조 → 인라인).

- 수식으로 만들던 열이라 **값이 있어도 지웁니다**(`DROP_HEADERS`). 사람이 적는 열은 예전처럼 비었을 때만 지웁니다.
- **발번시각**도 뺐습니다("엉 지워"). 광고세팅에도 빌더에도 쓰이지 않던 값입니다.
- **연령 · 소재유형**을 뺐습니다. 담당자는 뺐다가 다시 넣었습니다.
  사람이 채우는 칸은 **랜딩링크 · 목적 · 타겟팅 · 담당자**입니다.
  담당자는 UTM 빌더에도 칸을 만들어(이니셜 `ohy` · `ljm` · `lhj` · `khb` · `cyj`, 직접 입력 가능)
  적재할 때 함께 보냅니다. 광고명 뒤에 붙습니다.
  광고그룹명은 연령 자리가 빈 모양(`[naver-260829]_none_naver`)이 되고, 광고명은 뒤 두 토막이 빠집니다.
- 남은 열 16개: 행사명 · 파일명 · 매체 · 행사일자 · 상품명 · 행사채널 ·
  랜딩링크 · 목적 · **타겟팅** · 담당자 · LINK(GA) · NT · FM · 캠페인명 · 광고그룹명 · 광고명

**아직 안 한 것** — 메타 광고 세팅 화면은 여전히 `utm-builder` 시트를 봅니다.
적재시트를 보게 바꾸려면 그 화면의 조회 부분(열 번호로 읽는 곳)을 함께 고쳐야 합니다.

---

## 14. 매체별 성과 (메타 광고 대시보드)

> "워크스페이스 퍼포먼스 - 매체별성과에 api 로 광고대시보드 만들어 / 광고계정 선택할 수 있게 /
> 총광고비, 총결과, CPC, CTR, CPM, CPA 요약 / CPA는 트래픽광고는 빼고 /
> 게재: 활동중인 캠페인/광고그룹 리스트"

메뉴만 있고 대시보드를 보여 주던 **매체별 성과** 에 전용 화면을 만들었습니다.

**토큰을 어디에 둘지부터 정해야 했습니다** — 워크스페이스는 Firebase 정적 호스팅이라
브라우저에서 Graph API 를 직접 부르면 액세스 토큰이 페이지에 그대로 노출됩니다.
이미 쓰고 있는 Apps Script 웹 앱을 한 겹 세웠습니다.

```
브라우저 ──▶ Apps Script (META_ACCESS_TOKEN 보관) ──▶ 메타 Graph API
```

적재와 **같은 주소**를 씁니다. 보낸 것에 `action` 이 있으면 시트를 건드리지 않고 조회만 합니다
(`metaAccounts` · `metaReport`). 새 서버를 띄우지 않아 배포처가 늘지 않습니다.

### 화면

- **광고 계정 고르개** — 토큰으로 볼 수 있는 계정을 불러옵니다(`이름 (번호)`, 중지된 계정은 표시).
- **기간** — 오늘 · 어제 · 최근 7/14/30일 · 이번 달 · 지난 달 · 직접 지정.
  '최근 N일' 은 메타 관리자와 같이 **오늘을 넣지 않습니다**(오늘 수치는 계속 움직이므로).
- **요약 6칸** — 총광고비 · 총결과 · CPC · CTR · CPM · CPA.
- **게재** — 지금 켜져 있는 캠페인 목록. 줄을 누르면 그 캠페인의 **광고그룹이 펼쳐집니다.**
  표 복사(탭 구분)로 시트에 그대로 붙일 수 있습니다.
- 고른 계정 · 기간은 브라우저에 저장하고, **메뉴에 들어올 때 한 번만** 부릅니다.

### 숫자 정의

| | 규칙 |
|---|---|
| 총광고비 | 기간 안 전체 spend (지금 꺼진 캠페인도 포함) |
| 총결과 | **구매 + 장바구니 + 리드** |
| CPC · CTR | **링크 클릭** 기준 (`inline_link_clicks`) |
| CPM | 광고비 ÷ 노출 × 1000 |
| CPA | **트래픽 목적을 뺀** 광고비 ÷ 결과 |

- **겹쳐 세지 않습니다** — 메타는 같은 전환을 `omni_purchase` 와 `offsite_conversion.fb_pixel_purchase`
  양쪽으로 줍니다. 한 묶음에서 먼저 잡히는 하나만 셉니다.
- **꺼진 캠페인을 목록에서는 빼되 합계에서는 빼지 않습니다.** 게재 목록은 지금 켜진 것만 올리고,
  표 아래에 `지금 꺼져 있는 캠페인 N개 (₩X) 도 위 요약에 들어 있습니다` 라고 적어 둡니다.
- 트래픽 캠페인 줄의 CPA 칸은 숫자 대신 `트래픽` 이라고 표시합니다.

### 그 밖에

- 같은 계정 · 같은 기간은 **5분 동안 담아 뒀다가** 그대로 줍니다(메타 호출 제한 때문).
  **메타에서 새로 받기** 는 이 캐시를 건너뜁니다. 캐시 한 칸이 100KB 라 넘치면 담지 않고 지나갑니다.
- 토큰이 없으면 **넣는 방법을 화면에 안내**합니다(스크립트 속성 → 새 버전 배포).
- 날짜는 `toISOString()` 을 쓰지 않고 로컬 값으로 만듭니다. UTC 라 한국 시간 기준 하루가 밀립니다.
- `.content-tabs` 는 `display:flex` 가 `hidden` 을 이기므로 이 화면도 숨김 목록에 넣었습니다.

**확인** — jsdom 120가지. 화면 64가지(계정 목록 · 기본 기간이 어제까지 7일인지 · 요약 6칸 값 ·
게재 목록에 켜진 것만 오는지 · 광고그룹 펼치기 · 기간 바꾸기 · 표 복사 · 오류와 토큰 안내 ·
다른 메뉴 왕복)와 Apps Script 56가지(결과를 겹쳐 세지 않는지 · 성과가 없어도 켜져 있으면
목록에 넣는지 · 캐시 · 메타 오류 전달 · 토큰 없을 때).

### 14-1. 페이지 토큰이라 계정 목록을 못 읽던 것

배포하고 처음 물어보니 `Invalid OAuth access token - Cannot parse access token` 이 왔습니다.
`.env` 의 토큰을 메타에 직접 물어보니 토큰은 멀쩡한데 **`/me` 가 페이지로 잡혔습니다**(페이지 토큰).
페이지에는 `adaccounts` 가 없어 계정 **목록**만 못 가져오고, `act_…` 를 직접 부르면 정보도 인사이트도 잘 나옵니다.

- 목록 읽기에 실패하면 **정해 둔 계정 id 로 하나씩 물어봅니다**(`listedAccounts_`).
  기본값은 메타 광고 세팅 화면과 같은 8개(미닉스 · 컬리 · CJ · 오늘의집 · 네이버 · 쿠팡 · 무신사 · 29cm).
  스크립트 속성 `META_AD_ACCOUNTS` 에 쉼표로 적어 두면 그 목록이 우선합니다.
- 사용자 토큰으로 바꾸면 `/me/adaccounts` 가 되므로 **그때는 목록을 그대로 씁니다.** 코드는 안 고쳐도 됩니다.
- 계정 목록은 10분 담아 둡니다(계정마다 한 번씩 부르므로). **메타 연결 확인** 은 담아 둔 값을 지우고 진짜로 물어봅니다.
- 하나도 못 읽으면 메타가 준 이유를 그대로 올려 보냅니다.

**화면도 목록 없이 버티게 했습니다** — 계정 목록을 못 받으면 오류로 멈추지 말고
앱이 아는 계정 8개로 채우고 성과는 그대로 부릅니다. 위에 `기본 계정 8개로 채웠습니다` 라고 사유와 함께 적어 둡니다.
그 목록은 메타 광고 세팅 화면이 쓰던 것을 위로 올려(`AD_ACCOUNTS`) 두 화면이 함께 씁니다.

**배포 주소가 또 바뀌었습니다** — `새 배포` 를 누르면 주소가 새로 생깁니다(기존 배포의 `버전: 새 버전` 이라야 유지).
앱의 `SHEET_ENDPOINT` 를 새 주소로 옮겼습니다. 적재도 같은 주소를 쓰므로 함께 따라갑니다.

**남은 것** — Apps Script 에 토큰을 넣고 새 버전으로 배포해야 실제 숫자가 들어옵니다.
권한 승인용으로 `checkMetaToken` 을 뒀습니다 (`_` 로 끝나는 함수는 편집기 실행 목록에 뜨지 않습니다). 시트 **UTM** 메뉴에도 넣었습니다.
넣는 자리와 순서는 [google-apps-script.md](google-apps-script.md) 에 적어 두었습니다.

---

### 14-2. 구글 광고도 같은 화면에

> "구글도 똑같이 가능?" → "엉 부탁"

**된다는 것부터 확인했습니다** — [env.txt](env.txt) 의 자격 증명으로 실제 데이터를 뽑아 보고 붙였습니다.
(앳홈_미닉스 계정, 8/23~29: 광고비 7,109,656원 · 노출 1,686,057 · 클릭 4,859 · 전환 43)

- 리프레시 토큰 → 액세스 토큰 → Google Ads API `v22` 에 GAQL. **API 버전은 v22 만 열려 있습니다**(v16~v21 은 404).
- 계정 목록은 MCC 아래 `customer_client` 에서 읽고 **관리자 계정은 뺍니다.**
- 스크립트 속성 5개가 필요합니다(`GOOGLE_ADS_*`). 시트 메뉴에 **구글 연결 확인** 도 넣었습니다.

**화면은 매체 탭으로 갈랐습니다** — `Meta Ads` · `Google Ads`. 요약 6칸 · 게재 목록 · 표 복사는
**두 매체가 같은 코드**를 씁니다. Apps Script 가 구글 응답을 메타와 같은 모양으로 맞춰 주기 때문입니다.

| | 메타 | 구글 |
|---|---|---|
| 결과 | 구매 + 장바구니 + 리드 (행동) | 전환 카테고리 `PURCHASE` + `ADD_TO_CART` + `SUBMIT_LEAD_FORM` |
| CPC · CTR | 링크 클릭 | 클릭 (구글의 클릭이 링크 클릭입니다) |
| CPA 에서 뺄 것 | 트래픽 **목적** | **동영상** 유형 (브랜딩) |
| 목적 자리 | 캠페인 목적 | 캠페인 유형(검색 · 디스플레이 · 동영상 · 디맨드젠 · P맥스) |

**구글에는 캠페인 목적이 없습니다.** 그래서 CPA 에서 뺄 캠페인을 유형(`VIDEO`)으로 골랐습니다.
지금 계정은 브랜딩 영상(VVC/VRC)이 광고비의 60%라 이걸 빼지 않으면 CPA 가 크게 부풀려집니다.
다르게 가르고 싶으면 `SOURCES.google.skip` 한 줄만 고치면 됩니다(이름 규칙으로 가르는 것도 가능).

- 광고비 · 예산은 **마이크로 단위**로 와서 1,000,000 으로 나눕니다.
- 전환 카테고리가 하나도 안 잡히는 계정은 전환수를 그대로 씁니다.
- 고른 계정은 **매체마다 따로 기억합니다.** 탭을 오가도 각자 보던 계정으로 돌아옵니다.

**확인** — 화면 80가지 · 메타 56가지 · 구글 39가지. 구글 쪽은 마이크로 → 원 환산, 카테고리 합산에서
페이지뷰 같은 건 안 세는지, 관리자 계정을 빼는지, MCC 번호에서 숫자만 보내는지, 액세스 토큰을
한 번만 받는지(캐시), 속성이 빠졌을 때 그 이름을 알려 주는지까지 봤습니다.

- 속성 이름은 **접두사를 떼도 받습니다**(`CLIENT_ID` · `REFRESH_TOKEN` · `LOGIN_CUSTOMER_ID`).
  실제로 그렇게 넣어 두셔서, 이름을 고치게 하는 대신 코드가 세 가지 이름을 다 보게 했습니다.
  실패하면 **어떤 속성이 들어와 있는지**(이름과 길이만, 값은 안 보여 줍니다) 함께 알려 줍니다.

**남은 것** — 스크립트 속성 5개를 넣고 새 버전으로 배포해야 Google Ads 탭에 숫자가 들어옵니다.

---

### 14-3. 게재 목록 검색 · 필터

> "캠페인/ 광고그룹에 필터/ 검색할 수 있게 만들어줘"

캠페인이 120개(광고그룹 455개)까지 나오는 계정이 있어 목록에서 찾기가 어려웠습니다.

- **검색** — 캠페인 · 광고그룹 이름 어느 쪽이 걸려도 됩니다.
  **광고그룹이 걸리면 그 캠페인이 저절로 펼쳐지고, 걸린 광고그룹만 보입니다.**
  캠페인 이름이 걸린 경우에는 그 아래 광고그룹을 다 보여 줍니다.
- **목적 · 유형 칩** — 표에 올라온 것에서 뽑습니다(메타는 목적, 구글은 캠페인 유형). 여럿 고르면 OR 입니다.
- **꺼진 것도 보기** — 기간에 돌았지만 지금 꺼진 캠페인까지 표에 올립니다. 예산 자리에  이라고 적습니다.
- 걸러지면 오른쪽에  처럼 남은 수를 적어 둡니다. 매체 탭을 바꾸면 검색 · 칩은 비웁니다.

**한 글자마다 다시 그리면 커서가 날아갑니다.** 화면을 통째로 다시 그리는 구조라,
검색창의 커서 위치를 기억했다가 그린 뒤 되돌려 놓습니다().

**"광고그룹 N" 은 걸러진 수가 아니라 그 캠페인이 가진 수를 적습니다.** 검색으로 하나만 보이더라도
그 캠페인의 광고그룹이 하나로 줄어든 것은 아니기 때문입니다.

## 되짚어 볼 만한 문제들

작업 중 발견해 고친, 반복되기 쉬운 유형입니다.

| 문제 | 원인 | 대응 |
|---|---|---|
| 화면이 갱신되지 않음 | `index.html` 의 `app.js?v=5` 가 고정돼 브라우저가 옛 스크립트를 캐시 | JS·CSS 버전을 함께 올림 (`648a123`) |
| 숨겨야 할 요소가 보임 | CSS `display` 가 `hidden` 속성을 이김 | `[hidden]{display:none}` 을 함께 지정 |
| 버튼 클릭이 무시됨 | 텍스트 입력 blur 의 `change` 가 화면을 다시 그려 클릭이 삼켜짐 | 텍스트 칸은 `input` 에서만 반영 |
| 드롭다운이 열리지 않음 | 다시 그리며 떨어져 나온 요소를 "바깥 클릭"으로 오판 | `isConnected` 검사 추가 |
| 표가 카드 밖으로 넘침 | 대시보드용 전역 `table{min-width:1050px}` | 해당 표에서 해제 |
| 내려받은 `.ps1` 의 한글이 깨짐 | PowerShell 5.1 은 BOM 없는 파일을 ANSI 로 읽음 | 저장할 때 UTF-8 BOM 을 붙임 |
| 메뉴를 열면 화면이 아래로 튐 | 해시가 섹션 `id` 와 같아 브라우저가 그 요소로 스크롤 | 해시를 `id` 와 다르게 |

---

## 검증 방식

- **jsdom 자동 테스트 613개** — 렌더링·편집·필터·발번·저장·전송까지 회귀 검사
- **실제 브라우저(Chrome) 확인** — 레이아웃 측정, 스크린샷, 드래그·클립보드·iframe 로딩 등
  자동 테스트로는 확인할 수 없는 동작
- 테스트 스크립트는 저장소에 포함하지 않았습니다
