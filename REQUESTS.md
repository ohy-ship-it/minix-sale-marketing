# 작업 요청 기록

minix 통합 워크스페이스에 대한 요청과 처리 내용을 순서대로 정리한 문서입니다.
각 항목의 커밋 해시로 실제 변경을 확인할 수 있습니다.

---

## 1. 광고소재 기획 보드 (Notion 이관)

### 1-1. 카드 클릭 편집
> "생성된 카드 클릭 시 수정할 수 있게 해줘"

- 카드 클릭 → 편집 열기 (`4a55b32`)
- **함께 고친 것**: 수정할 때마다 클릭 리스너가 중복 등록되던 문제, 드래그 직후 편집창이 열리던 문제,
  새로고침 때 기본 카드가 두 배로 늘어나던 문제

### 1-2. Notion 스키마로 재구축
> "노션 편집기능 줬거든? 똑같이 구현해" + 공유 시트 링크

- 공개 링크의 내부 API로 컬렉션 스키마와 행 5건을 그대로 읽어 이관 (`e7ad3dc`)
- 속성 10종을 원본과 동일한 이름·타입·옵션·색상으로 구현
  (이름 / 행사명 / 상태 / 매체 / 행사채널 / 담당자 / 행사일자 / 희망전달날짜 / SKU / 세팅)
- 카드 클릭 시 **우측 사이드 피크** 패널, 저장 버튼 없이 입력 즉시 반영
- 상태 변경·드래그로 컬럼 이동, 기간 날짜, 카드 삭제, Esc·배경 클릭 닫기
- **함께 고친 것**: 사용자 입력이 이스케이프되지 않아 제목에 태그를 넣으면 실행되던 문제

### 1-3. 화면 정리
> "이 위에 있는거 없애고, 담당자가 본인 이름 누르면 본인 이름이 있는 카드만 뜰 수 있게"

- 보드 화면에서 상단 헤더 숨김, 담당자 필터 추가 (`fe84a1f`)
- 툴바를 **요청하기** 버튼 하나만 남김 (`df50043`)

### 1-4. 속성 정리
> "카드 속성순서 바꿔줘 / 세팅 체크박스는 없애"

- 상태 → SKU → 행사채널 → 행사명 → 담당자 → 행사일정 → 전달희망일정 순서로 (`9d401c7`)
- 세팅 체크박스 제거
- 이후 카드 표시는 **SKU / 행사채널 / 담당자 / 전달희망일정** 만 (`5db74b0`)
- 최종적으로 **SKU 를 맨 위로, 행사채널은 숨김** (`c20c2c9`)

### 1-5. 요청 완료 버튼
> "요청완료하기 버튼 넣어줘"

- 상세 패널 하단에 sticky 버튼, 제목이 비면 닫히지 않고 안내 (`a83397f`)

### 1-6. 필터
> "필터에 년도도 추가" / "SKU별 필터도 만들어줘"

- 담당자 필터 아래 SKU 필터 (`3da6150`) — 같은 줄은 OR, 줄 사이는 AND
- 주간 목록에 년도 필터 (`20f4f70`)

---

## 2. 매체별 요청사항 칸

> "매체 속성 추가하면 그 아래에 매체별로 요청사항 쓸 수 있는 칸 만들어줘"

- 선택한 매체마다 색상이 다른 입력 칸 생성, 기본 문구 자동 삽입 (`aa07da1`)
- **소재 갯수** 칸 추가 (`c0a7b43`) → 숫자만 받던 것을 자유 입력으로 (`c3066d7`)
- 매체 없이도 쓰는 자유 입력란 (`26d3a33`)
- 칸 순서 드래그 변경 + 매체별 **전달 희망일정** (`d8c72ff`)
- 전달 희망일정을 **캘린더 선택**으로, 모든 날짜에 **요일 표기** (`c5ccc39`)
- 칸 삭제 버튼 (`30c068a`) → 오른쪽 끝으로 정렬 (`a0bf199`)

### 2-1. 집계
> "SKU별로 몇건인지" / "담당자별 소재갯수 더해서"

- 보드 상단에 **SKU 건수** 요약 (`20f4f70`)
- 매체별 소재 갯수를 합산한 **소재 갯수** 카드 속성 (`2e380b8`)
- 행사별 건수 → **담당자별 소재 갯수** 로 교체 (`c20c2c9`)

---

## 3. 주간 소재요청 목록

> "이 보드 주차별로 만들예정이거든? 어떻게 하면 깔끔하게 정리할 수 있을까?"

**판단**: 주차마다 독립 보드를 두는 구조를 선택 (카드에 주차 속성을 두는 방식과 비교 후)

- 주간 목록 → 클릭하면 해당 주차 보드 (`a3d8900`)
- 열 구성: 월 / 주차 / 공유링크 (`d8f5f22`) → 월 / 주차 / 건수 / 수정 / 공유 / 삭제 (`28f510b`)
  → 수정 / 공유 / 삭제 순서로 (`981aaf4`)
- 건수는 **SKU 기준**으로 집계
- 월·주차 필터, 주차는 1~5주차 고정, 추가 시 드롭다운 선택 (`32078e5`, `62b286b`)
- 공유링크는 복사 버튼만 노출

---

## 4. 반응형 · 정렬

> "창 크기 조절했을 때, 맞춰서 보여지게" / "세로정렬 확인하고 제대로맞춰" / "간격이 너무 넓음"

실제 브라우저로 5개 폭(1440·1200·900·700·480px)에서 측정하며 수정 (`20f4f70`, `40c81e1`, `62b286b`)

- `.main` 의 `min-width:800px` 제거 — 창을 줄여도 페이지가 가로로 밀리지 않게
- 폭에 따라 보드 컬럼·사이드바·상세 패널 단계별 대응
- **발견해 고친 문제들**
  - 전역 `td{text-align:center}` 때문에 표의 값이 헤더와 어긋나 있었음
  - 사이드바 메뉴가 좁은 폭에서 한 글자씩 세로로 쪼개지던 문제
  - 열 폭 분배가 특정 열에 몰려 값이 흩어지던 문제

---

## 5. 사이드바

> "콘텐츠 밑에 광고소재 파일명, 광고소재 T&D 추가 / KOL라이브 위에 마케팅팀 드롭다운"

- 콘텐츠 하위에 2개 추가, 마케팅팀 그룹 신설 후 KOL라이브 이동 (`6943519`)

---

## 6. 광고소재 파일명 생성기

> "광고소재 파일명을 만드는 아래 구글시트보고 구현해봐"

공개 링크의 시트 12개 탭을 모두 읽어 명명 규칙을 추출 (`b94c1df`)

| 항목 | 규칙 |
|---|---|
| 최종행사명 | `YYMMDD_상품명_행사채널_행사명` |
| 파일명 | `메시지코드-순번` |

- 메시지 유형 **58종**과 코드, 코드별 **마지막 발번 번호**(benefit 535, urgency 448 …)를 시트에서 그대로 이식
- 이후 요청에 따라 파일명 발번만 남기고 단순화 (`f4d4338`)
- 메시지 유형을 고르면 **즉시 발번** (`90f0f70`)
- 입력 칸: 행사일자(캘린더) / 행사채널 / 상품명 / 행사명 / 매체 / 메시지 유형 (`3ff0368`, `bafbcae`, `6ae1ec4`)
- **(세로)** 처리: 자동 생성 → 요청 시 생성 → 체크박스 토글로 변경 (`e703be9`, `265b354`, `d3d82ba`)
  체크하면 목록의 각 파일명 아래에 `(세로)` 가 붙고, 해제하면 사라짐
  건수·복사·시트 적재에서는 제외
- **전체 지우기**: 입력값 + 발번 목록 모두 비움, 번호는 되돌아가지 않음 (`28bf3c7`, `3469c0a`)

### 6-1. 메시지 유형 직접 추가
> "직접 작성하기 항목 만들어주고 팝업으로 기재 (한글·특수문자·띄어쓰기 안됨)"

- 처음엔 `window.prompt` 로 구현 (`d851026`) → **화면 안 모달**로 교체 (`8184f83`)
- 메시지 유형명(한글 가능) + 파일명 코드(영문·숫자만) 를 함께 입력받아 등록
- 등록한 유형은 드롭다운 하위 항목으로 남고, 다른 유형처럼 번호가 올라감
- 삭제 버튼을 **드롭다운 항목 옆**에 넣기 위해 `<select>` 를 직접 만든 드롭다운으로 교체 (`90ff1bb`, `c6ece7f`)

---

## 7. 구글시트 적재

> "최종완료 버튼 누르면 아래 구글시트에 적재해줄 수 있어?"

브라우저에서 시트에 직접 쓰려면 인증이 필요해, 시트 쪽에 Apps Script 웹 앱을 두는 방식으로 연결 (`265b354`)

- [Code.gs](Code.gs) · [google-apps-script.md](google-apps-script.md) 로 설정 방법 제공
- 배포 주소를 코드에 고정, 실제 적재까지 확인 (`f31cf48`, `1393ba4`)
- 적재 항목: **파일명 / 매체 / 메시지 유형 / 최종행사명 / 발번시각** (`e1fef32`)
  앱이 열 정의를 함께 보내므로 앞으로 열이 늘어도 재배포 불필요
- 버튼을 **적재확인** 으로 바꾸고 누르면 시트를 새 탭으로 열도록

**중간에 걸렸던 것**
- 첫 배포가 도메인 제한이라 로그인 페이지로 넘어가 요청이 막힘 → 액세스 권한을 "모든 사용자"로 재배포
- 문서(.md)를 통째로 붙여넣어 `SyntaxError` → 코드만 담은 `Code.gs` 파일 분리

---

## 8. 광고소재 검수

> "https://ad-creative-checker.onrender.com/ 이 사이트 추가해줄 수 있어?"

- 임베드 차단 헤더가 없어 화면 안에 iframe 으로 삽입 (`f04c855`)
- 메뉴에 들어갈 때만 로딩(무료 플랜이라 첫 로딩이 느림), 새로고침·새 탭 열기 버튼 제공
- **고친 것**: `display:flex` 가 `hidden` 속성을 덮어써서 다른 화면에도 보이던 문제 (`2327c81`)

---

## 되짚어 볼 만한 문제들

작업 중 발견해 고친, 반복되기 쉬운 유형입니다.

| 문제 | 원인 | 대응 |
|---|---|---|
| 화면이 갱신되지 않음 | `index.html` 의 `app.js?v=5` 가 고정돼 브라우저가 옛 스크립트를 캐시 | JS·CSS 버전을 함께 올림 (`648a123`) |
| 숨겨야 할 요소가 보임 | CSS `display` 가 `hidden` 속성을 이김 | `[hidden]{display:none}` 을 함께 지정 |
| 버튼 클릭이 무시됨 | 텍스트 입력 blur 의 `change` 가 화면을 다시 그려 클릭이 삼켜짐 | 텍스트 칸은 `input` 에서만 반영 |
| 드롭다운이 열리지 않음 | 다시 그리며 떨어져 나온 요소를 "바깥 클릭"으로 오판 | `isConnected` 검사 추가 |
| 표가 카드 밖으로 넘침 | 대시보드용 전역 `table{min-width:1050px}` | 해당 표에서 해제 |

---

## 검증 방식

- **jsdom 자동 테스트 410개** — 렌더링·편집·필터·발번·저장·전송까지 회귀 검사
- **실제 브라우저(Chrome) 확인** — 레이아웃 측정, 스크린샷, 드래그·클립보드·iframe 로딩 등
  자동 테스트로는 확인할 수 없는 동작
- 테스트 스크립트는 저장소에 포함하지 않았습니다
