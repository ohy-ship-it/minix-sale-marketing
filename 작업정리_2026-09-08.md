# 작업 정리 — 2026년 9월 7~8일

두 저장소를 함께 손봤습니다.

| 저장소 | 무엇 | 배포 |
|---|---|---|
| `ohy-ship-it/minix-sale-marketing` | 마케팅팀 워크스페이스 (`app.js` · `index.html` · `styles.css` · `Code.gs`) | 파이어베이스 — `minix-salemarketing.web.app` |
| `wnh-rgb/my-pages` | 세일즈 통합 워크스페이스 | Render — `minix-workspace.onrender.com` |

`Code.gs` 는 손댈 때마다 **Apps Script 재배포가 따로 필요**합니다. 나머지는 제가 배포했습니다.

---

## 1 · 매체별 성과 — 단계(사전 · 당일 · 사후) 나누기

### 왜 세 번 바꿨나

처음에는 **광고그룹을 체크해서** 단계를 붙였습니다. 그런데 한 캠페인이 사전에도 당일에도
걸쳐 있어서 광고그룹 단위로는 가를 수 없었습니다. 그래서 단계를 겹쳐 붙이게 했더니
합계가 맞지 않았고(같은 광고그룹이 두 단계에 들어감), 결국 **날짜로 가르는 방식**으로 갔습니다.

### 지금 방식 — 날짜로 가른다

- 전매체 검색으로 찾은 뒤 **사전 · 당일 · 사후 기간을 각각 적으면** 그 단계가 바로 채워집니다. 누를 단추가 없습니다.
- 단계마다 **그 기간만 매체에 따로 물어** 옵니다. 매체가 그 날짜 범위만 합쳐서 주므로
  **하루가 한 단계에만** 들어가고, 사전 + 당일 + 사후가 조회 합계와 그대로 맞습니다.
- 새 API 는 만들지 않았습니다 — 있는 리포트 호출을 기간만 바꿔 다시 부릅니다.
- 값은 **단계 날짜에만** 매여 있어서, 조회 기간을 바꿔 다시 찾아도 숫자가 움직이지 않습니다.
  찾는 말이 바뀔 때만 버리고 새로 받습니다.
- `단계 날짜 지우기` 로 세 쌍을 한 번에 비웁니다. 날짜는 브라우저에 남습니다.

### 알려 주는 것

- 단계 날짜가 **서로 겹치면** — 같은 하루가 두 번 들어가 합계가 부푼다고 경고합니다.
- 조회 기간을 **다 덮지 않으면** — 빠진 광고비가 얼마인지 적습니다.
- 조회 기간을 **넘어서면** — 단계 합이 얼마나 더 큰지 적습니다.
- 딱 맞으면 `단계 날짜가 조회 기간을 그대로 덮습니다` 라고 확인해 줍니다.

### 보여 주는 모양

- 맨 위 **단계별 합계** 표 (사전 · 당일 · 사후 + 총합). 총합은 세 줄을 그대로 다 더한 값입니다.
- 단계마다 블록. 그 안은 **캠페인명으로 합쳐** 한 줄씩 (광고그룹 몇 개인지 함께 적습니다).
- 비율(CTR · CPC · CVR · ROAS)은 **더한 값에서 다시 계산**합니다. 줄마다의 비율을 평균 내면 틀립니다.
- 찾기는 그대로 **광고그룹 이름**으로 합니다 — 이름 규칙이 매체마다 같아 한 행사를 모을 수 있습니다.

---

## 2 · 전매체 검색 — 계정을 다 훑는다

계정을 골라 두지 않으면 **첫 계정 하나만** 봐서 다른 계정에 올린 행사가 안 잡혔습니다.
이제 그 매체에서 볼 수 있는 계정을 **전부** 물어 이름이 맞는 광고그룹을 모읍니다.

- 메타는 계정이 **8곳**입니다. `cj-260901` 은 그중 `앳홈 미닉스(CJ)` 한 곳에만 있습니다.
- 권한이 없어 안 열리는 계정이 있어도 나머지는 그대로 찾고, 그 줄에 `못 읽은 계정: …` 을 적습니다.
- 줄에 `계정 8곳 훑음` 을 적어 둡니다 — 한 곳만 본 것처럼 보이던 오해를 없앴습니다.
- 단계별로 볼 때는 **검색에서 걸린 계정만** 다시 묻습니다.

---

## 3 · 광고비 기준

- **부가세 10% 를 빼는 것은 네이버 GFA 뿐**입니다. 카카오모먼트는 메타 · 구글처럼 매체가 준 값 그대로 씁니다.
  (`Code.gs` 의 `kakaoMetrics_` 한 줄 — 카카오의 모든 경로가 그 함수를 지납니다)
- 프로모션 MIX 의 **부가세 설정**에서는 **GFA 만 체크하지 마세요.** 나머지는 MIX 쪽 설정을 따르시면 됩니다.

---

## 4 · GFA 적재가 멈추면 알려 준다

`cj-260901` 광고비가 적게 나온 원인은 계산이 아니라 **적재 실패**였습니다.

```
2026-09-03 14:01 · 성공 · 510줄
2026-09-04 12:16 · 실패 · 로그인이 만료됐습니다
…
2026-09-07 14:03 · 실패 · 로그인이 만료됐습니다
```

로그인이 만료된 것이 아니라 **적재용 크롬 창이 안 떠 있었습니다.** 창을 띄우니 이미 로그인된
상태였고, 9/1~9/7 을 다시 받아 채웠습니다(386줄). cj-260901 광고그룹 합이
₩4,217,610 → **₩6,847,857** 로 늘었습니다.

- **그 크롬 창을 닫지 마세요.** 최소화만 해 두면 예약 적재(09 · 14 · 19시)가 그 창에 붙습니다.
- PC 를 껐다 켜면 `gfa_login.bat` 을 한 번 실행해 창만 띄우면 됩니다.
- 그리고 **적재가 빠진 날 경고**를 전매체 검색과 단계 표에도 붙였습니다.
  전에는 네이버 GFA 탭에만 있어서 조용히 적은 값이 나왔습니다.

---

## 5 · 프로모션 MIX 로 보내기

전매체 결과를 MIX 에 넣는 길입니다. MIX 는 **매체마다 CSV 하나**를 올려 받습니다.

- 단계 블록 아래에 **매체 × 단계마다 파일 하나**를 내려받는 단추가 생깁니다.
- 열 이름은 `소재 · 노출 · 클릭 · 광고비 · 전환 · 매출` — MIX 의 컬럼 매핑 정규식에 그대로 걸려서
  **사람이 매핑을 손댈 것이 없습니다.**
- **일자 열은 일부러 뺐습니다.** MIX 는 일자가 있으면 자기 Phase 날짜표로 단계를 다시 매기고,
  그 판정이 업로드할 때 고른 Phase 를 이깁니다(`mix.html` · `phaseOfUploadRowInSlot`).
- MIX 에는 **사후 칸이 없습니다**(사전 · 사전(알림) · 당일 · 상시). 사후 파일은 상시나 Phase 미지정으로 올리세요.

---

## 6 · 광고소재 기획 — 9월 1주차

노션 `[통합] 광고소재요청 보드` 의 `26년 9월 1주차(9/1~6)` 표 10줄을 그대로 옮겼습니다.
매체 · 담당자 · SKU · 행사채널 · 상태 모두 우리 화면에 이미 있는 값이라 그대로 들어갔습니다.

- 브라우저마다 **한 번만** 넣습니다 — 지우고 새로 고쳐도 되살아나지 않습니다.
- 주차 목록을 **끌어서 순서**를 바꿉니다(손잡이 ⠿). 새로고침해도 순서가 남습니다.

**주차 목록은 서버가 아니라 그 브라우저에 저장됩니다.** 주소(`web.app` / `firebaseapp.com`)나
컴퓨터가 다르면 목록도 따로 갑니다. 원하시면 ① 주소 하나로 몰기 ② 시트에 저장하기 중
하나를 해 두겠습니다. 탭 두 개를 열어 두면 옛 목록이 새 주차를 덮어쓰는 허점도 아직 있습니다.

---

## 7 · 세일즈 워크스페이스 — 행사별 결과 (새 화면)

`세일즈 팀 › 세일즈마케팅 파트 › 행사별 결과` 를 새로 만들었습니다.
프로모션 리스트(`minix-list`)를 **그대로 복사**해 파일을 나눠 뒀습니다 — 이쪽을 고쳐도 원본은 그대로입니다.

- **마케팅 지원 체크된 행사만** 처음부터 보입니다(단추를 다시 누르면 전체).
- `결과(성과입력)` · `데이터 입력(MD/마케터 완료)` 칼럼을 걷어내고 **`매체믹스` · `매체결과`** 를 넣었습니다.
- 카드를 펼치면 탭이 다섯입니다 — 요약 / 운영·마케팅 의뢰 / 미디어커머스팀 전달용 / **매체믹스** / **매체결과**.
- `매체믹스` 는 그 행사의 프로모션 MIX 로 갑니다. 목록의 매체결과 금액을 누르면 매체결과 탭이 열립니다.
- 공용 파일(`index.html` 은 메뉴 5줄만, `server.js` 는 안 건드림)을 거의 손대지 않았습니다.

### 매체결과 — 마케팅팀 값을 넣는다

마케팅팀 워크스페이스는 다른 도메인이라 그 서버 API 를 부를 수 없습니다. 그런데 이 화면은
**그 서버 안에서** 돌아가니 세션이 있습니다 — 사람이 파일만 옮겨 주면 읽기 · 저장 · 표시는 이 화면에서 다 합니다.

**흐름**

1. 마케팅팀: 매체별 성과 › 전매체 검색 → 광고그룹 이름으로 찾기 → 사전 · 당일 · 사후 날짜 넣기 → **`세일즈용 파일`** 받기
2. 세일즈마케팅: 행사별 결과 → 그 행사 줄의 **`＋ 파일 넣기`** (또는 카드 펼쳐 `매체결과` 탭) → 그 파일 고르기

**왜 엑셀이 아니라 따로 만든 파일인가** — 엑셀(CSV)은 사람이 보기 좋게 납작하게 편 것이라
매체 · 광고그룹 · 캠페인 · 집행일자가 다 실리지 않습니다. `세일즈용 파일(.json)` 에는
**화면이 쓰는 값이 그대로** 들어갑니다. 비율은 담지 않습니다 — 받는 쪽에서 더한 값으로 다시 계산해야
합계가 맞습니다. 엑셀(.csv)도 계속 받지만 그때는 단계 합계만 보입니다.

**보여 주는 것** (마케팅팀 화면과 같은 차례 · 같은 기준)

- 맨 위 **총지표** — 총광고비 · 총판매수 · 총매출 · 총CPS · 총ROAS · 총CVR · 총노출수 · 총클릭수 · 총CPC · 총CTR
- **총판매수 · 총매출은 사람이 적습니다.** 매체가 세는 구매 · 전환값과 실제 판매가 다르기 때문입니다.
  적으면 그 값으로 CPS · ROAS · CVR 을 다시 세고, 비우면 매체 값을 씁니다(놓인 값으로 보입니다).
  적은 값은 `ops.mediaManual` 에 **따로** 둡니다 — 파일을 다시 넣어도 안 지워집니다.
- 단계별 합계 표, 단계마다 매체 · 캠페인 블록(왼쪽 색 띠 — 사전 노랑 · 당일 초록 · 사후 파랑)
- 결과 = results, CVR = 구매 ÷ 클릭, CPA = 광고비 ÷ 결과 — 마케팅팀 화면과 같은 기준입니다.

**확인한 값** (실제 계정 · `cj-260901`)

```
단계   기간             광고비      노출      클릭   CTR   CPC   CPM  결과   CPA        전환값     ROAS
사전   09/01 ~ 09/01  ₩3,073,774  2,166,219   8,575 0.40% ₩358 ₩1,419   1 ₩3,073,774   ₩294,000    9.6%
당일   09/02 ~ 09/02  ₩4,810,625  1,633,846   7,823 0.48% ₩615 ₩2,944  87   ₩55,295 ₩22,290,920  463.4%
사후   09/03 ~ 09/04  ₩8,444,389  3,540,364  16,675 0.47% ₩506 ₩2,385  80  ₩105,555 ₩22,003,900  260.6%
총합                 ₩16,328,788  7,340,429  33,073 0.45% ₩494 ₩2,225 168   ₩97,195 ₩44,588,820  273.1%
```

마케팅팀 엑셀의 총합 줄과 한 자리도 다르지 않습니다.
수기로 판매수 400 · 매출 52,000,000 을 넣으면 CPS ₩40,822 · ROAS 318.5% · CVR 1.21% 로 다시 셉니다.

---

## 8 · 그 밖에

- **행사별 성과 하위에 프로모션 리스트를 iframe 으로 붙였다가 걷어냈습니다**(요청). 지금은 없습니다.
- 우리 워크스페이스에서 세일즈 서버로 **바로 저장하는 길은 없습니다** — 다른 도메인이고 세션이 없습니다.
  파일로 옮기는 지금 방식이 그 제약을 피하는 길입니다. 자동으로 넘기려면 그쪽 셸이 우리 화면을
  이미 iframe 으로 쓰고 있으니(`?embed=1#media-report`) `postMessage` 로 넘기고 부모가 저장하는 방법이 있습니다.
  그건 `my-pages` 의 공용 파일을 고치는 일이라 하지 않았습니다.

---

## 남은 것 · 정할 것

- [ ] `브티나는생활 MLC` 에 넣어 두신 값은 옛 엑셀이라 단계 합계만 보입니다. `↻` 로 세일즈용 파일을 넣으면 전부 채워집니다.
- [ ] 미리보기용 중계기와 크롬 창, 바탕화면 `전매체엑셀` 폴더 — 이제 실서비스로 보시면 되니 정리할까요?
- [ ] 주차 목록 저장 자리 — 주소 하나로 몰기 / 시트에 저장하기 중 무엇으로 할까요?
- [ ] 탭 두 개를 열어 두면 주차가 덮어써지는 허점 — 합쳐서 저장하게 막을까요?
- [ ] `Code.gs` 의 안 쓰는 함수(`tndAppend_` · `tndTop_` · `filenameRecent_`) — 다음 재배포 때 걷어낼까요?
