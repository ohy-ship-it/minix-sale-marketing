# 광고소재 파일명 → 구글시트 적재 · UTM 자동생성

`광고소재 파일명` 화면의 **최종완료** 버튼이 구글시트에 바로 적재하도록 연결하는 방법입니다.
적재된 행에는 [UTM 이 자동으로 붙습니다](#utm-자동생성).
브라우저에서 시트에 직접 쓰려면 인증이 필요해서, 시트 쪽에 받아주는 창구(Apps Script 웹 앱)를 한 번만 만들어 두면 됩니다.

대상 시트:
https://docs.google.com/spreadsheets/d/1-IrBGbuQmcQ9Za1LCZKfV6XUaGIV5gtut5npHw0Gu_E/edit

## 1. Apps Script 만들기

1. 위 시트를 연다
2. 상단 메뉴 **확장 프로그램 → Apps Script**

   > 따로 설치할 게 없습니다. 시트 제목 아래 메뉴 줄에 이미 있습니다.
   > `파일  수정  보기  삽입  서식  데이터  도구  **확장 프로그램**  도움말`
   >
   > - 메뉴가 안 보이면 시트가 **보기 전용**으로 열린 경우입니다. 편집 권한이 필요합니다.
   > - 계정 언어가 영어면 **Extensions → Apps Script** 입니다.
3. 기본 코드를 **모두 지우고**, 저장소의 [Code.gs](Code.gs) 파일 내용을 그대로 붙여넣는다

   > 이 문서(.md) 를 통째로 붙여넣으면 안 됩니다. 설명 문장 때문에
   > `SyntaxError: Invalid or unexpected token` 이 납니다.
   > **Code.gs 파일만 열어 전체 선택(Ctrl+A) → 복사** 하세요.
   > 붙여넣은 뒤 첫 줄이 `var SHEET_ID = ...` 로 시작하면 제대로 된 것입니다.

4. 저장(디스크 아이콘)

## 2. 웹 앱으로 배포

1. 우측 상단 **배포 → 새 배포**
2. 톱니바퀴 → **웹 앱** 선택
3. 설정
   - 설명: 아무거나 (예: 파일명 적재)
   - **다음 사용자로 실행: 나**
   - **액세스 권한이 있는 사용자: 모든 사용자**
4. **배포** → 권한 승인(본인 계정)
5. 나오는 **웹 앱 URL**을 복사 (`https://script.google.com/macros/s/AKfy.../exec` 형태)

## 3. 앱에 주소 넣기

> 배포 주소는 이미 `app.js` 의 `ENDPOINT` 상수에 들어가 있습니다.
> 주소가 바뀌면 그 상수만 고치면 됩니다.

## 열이 바뀌었을 때

앱이 **어떤 열을 어떤 이름으로 쓸지 함께 보내므로**, 앞으로 열이 늘어도 스크립트를 다시 배포할 필요가 없습니다.
값은 보낸 차례가 아니라 **시트 헤더의 이름을 찾아 그 자리에** 들어갑니다. (UTM 빌더는 랜딩링크 · 목적까지 함께 보냅니다)
현재 항목: **행사명 · 파일명 · 매체 · 행사일자 · 상품명 · 행사채널**
(그 뒤의 UTM 열은 스크립트가 알아서 붙입니다. 아래 참고)

> 단, 이 방식으로 바꾼 [Code.gs](Code.gs) 를 **한 번은 배포**해야 합니다.
> 다시 복사해 붙여넣고 저장 → **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포**
>
> 이미 만들어진 헤더 행은 자동으로 바뀌지 않습니다. 기존 데이터를 지워도 된다면 **시트를 통째로 비우면**
> 다음 적재 때 새 헤더가 자동으로 만들어집니다.

## 파트별 탭

적재는 **파트 탭**으로 갈립니다. UTM 빌더에서 고른 파트가 그대로 탭 이름입니다.

| 파트 | 탭 |
|---|---|
| 세일즈마케팅 | `세일즈마케팅` |
| 더플렌더_파트 | `더플렌더_파트` |
| 생활가전_파트 | `생활가전_파트` |

- 한 번에 여러 파트를 담아 **최종완료** 하면 파트별로 나눠 보냅니다.
- 탭이 없으면 만들어 줍니다. 각 탭은 열 구성과 수식이 모두 같습니다.
- **UTM → 전체 행 다시 계산** 은 세 탭을 모두 돕니다.
- `광고소재 파일명` 화면의 적재는 파트를 고르지 않아 **세일즈마케팅** 탭으로 들어갑니다.

## UTM 자동생성

파일명이 시트에 들어오면 UTM 이 그 줄에서 바로 만들어집니다.
규칙과 열 차례는 [utm-builder 시트](https://docs.google.com/spreadsheets/d/1K9kGdlfuHCp-VNaKUuGMYi9B_rRbW0rY1lg_NFYMv8s/edit)
의 `utm-builder` · `index` 탭을 그대로 옮긴 것입니다.

### 열 구성

| 열 | 채우는 주체 | 어디서 오나 |
|---|---|---|
| 행사명 | 앱 | 파일명 화면의 행사명 (시트에서 고쳐 씁니다) |
| 파일명 | 앱 | 메시지 유형 → `메시지코드-순번` |
| 매체 | 앱 | 매체 선택 |
| 행사일자 · 상품명 · 행사채널 | 앱 | 각각 한 칸 |
| **랜딩링크 · 목적** | **사람** | 파일명만으로는 알 수 없음 |
| **타겟팅** | **사람** | 광고그룹명에만 쓰입니다 |
| 담당자 | 앱 · 사람 | UTM 빌더에서 이니셜로 고르거나 직접 적습니다 |
| LINK(GA) · NT(일반) · NT(쇼핑스토리) · FM | 수식 | 랜딩링크 + 각 파라미터 |
| 캠페인명 · 광고그룹명 · 광고명 | 수식 | 광고자동세팅용 이름 |

**`utm_source` 같은 낱개 열은 두지 않습니다.** 링크 안에 이미 들어 있어 그 자리에서 계산합니다.
(`utm_content` 는 `광고명` 과 같은 값이라 광고명 한 열만 둡니다)

### 빌더에는 있는데 여기엔 없는 것

| utm-builder 열 | 왜 없나 |
|---|---|
| 광고명 앞 일련번호(`6581_`) | 그 시트의 행 번호라 옮길 수 없음. 우리는 파일명이 그 역할 |
| 상품(always 등) | 칸이 없음 → 캠페인명 뒤에 붙는 토막이 빠짐 |

### 만들어지는 값

| 항목 | 규칙 |
|---|---|
| `LINK(GA)` | 랜딩링크 + UTM 파라미터 — **랜딩링크를 채워야 나옵니다** |
| `NT(일반)` | 랜딩링크 + `?nt_source`(=utm_source_utm_medium) · `nt_medium`(=광고명) |
| `NT(쇼핑스토리)` | 랜딩링크 + `?nt_source` · `nt_medium` · `nt_detail` — utm-builder 의 원래 3개짜리 형태 |
| `FM(쇼핑라이브)` | 랜딩링크 + `?fm`(=utm_source) · `sn`(=utm_medium) · `ea`(=utm_content) |
| `캠페인명` | `utm_source_제품 정식명_목적(영문)` — 예: `facebook_미닉스 더 플렌더(mini)_purchase` |
| `광고그룹명` | `[행사명]_타겟팅_매출채널` — 빈 칸도 자리를 지킵니다 (`[naver-260829]_none_naver`) |
| `광고명` | `행사일자_제품코드_파일명_담당자` (= utm_content) |

링크 안에 들어가는 값은 이렇게 만들어집니다 — `utm_source` · `utm_medium` 은 매체 대응표,
`utm_campaign` 은 목적(영문), `utm_content` 는 광고명, `utm_term` 은 검색매체(`sa`)면 `{keyword}`.

**광고자동세팅용 이름**은 각 칸을 그대로 씁니다.

```
행사명 naver-260829-mini  행사일자 2026-08-29  상품명 더플렌더mini  행사채널 네이버
타겟팅 none  담당자 ljm  파일명 celebrity-77
        ↓
캠페인명    facebook_미닉스 더 플렌더(mini)_purchase
광고그룹명   [naver-260829-mini]_none_naver
광고명      20260829_flender-mini_celebrity-77_ljm
```

빌더 시트와 같은 모양입니다. 빌더는 광고명 앞에 행 번호(`6581_`), 뒤에 소재유형 · 담당자(`_image_ljm`)가
더 붙고 광고그룹명에 연령(`25-54`)이 들어가는데, 그 칸들은 두지 않기로 했습니다.

`utm_content` 는 **행사일자 · 상품명** 뒤에 **파일명을 통째로** 붙입니다.
메시지코드는 번호까지가 한 덩어리(`benefit-537`)라 자르지 않습니다.

```
파일명 benefit-537   매체 GFA-피드   행사일자 2026-08-29   상품명 더플렌더mini
랜딩링크 https://minix.life/   목적 purchase
        ↓
utm_content 20260829_flender-mini_benefit-537
LINK(GA)    https://minix.life/?utm_source=gfa&utm_medium=feed&utm_campaign=purchase&utm_content=20260829_flender-mini_benefit-537
NT(일반)      https://minix.life/?nt_source=gfa_feed&nt_medium=20260829_flender-mini_benefit-537
NT(쇼핑스토리) https://minix.life/?nt_source=gfa&nt_medium=feed&nt_detail=20260829_flender-mini_benefit-537
FM          https://minix.life/?fm=gfa&sn=feed&ea=20260829_flender-mini_benefit-537
```

세 링크 모두 **랜딩링크**를 앞에 붙입니다. 네이버스토어 · 쇼핑라이브로 보낼 줄이면
랜딩링크 칸에 그 주소를 넣으면 됩니다. (utm-builder 시트도 url 한 칸으로 세 링크를 만듭니다)

랜딩링크가 **비어 있으면 `?` 로 시작하는 파라미터만** 남으니, 주소 뒤에 그대로 이어 붙여도 됩니다.

```
랜딩링크 없음   ?fm=gfa&sn=feed&ea=…
랜딩링크 있음   https://shoppinglive.naver.com/livebridge/1942722?fm=gfa&sn=feed&ea=…
```

### 설정 시트

대응표는 코드가 아니라 같은 문서의 **설정** 탭에 있습니다. 매체나 상품이 늘면 여기만 고치면 됩니다.
(재배포 필요 없음. 탭이 없으면 처음 적재할 때 기본값으로 만들어집니다.)

| A · B · C | E · F | H · I | K · L |
|---|---|---|---|
| 매체 → utm_source · utm_medium | 목적 → 목적(영문) | 상품명 → 제품코드 · **제품 정식명** | 행사채널 → 매출채널 |
| 메타 → facebook · display | 구매 → purchase | 더플렌더mini → flender-mini · 미닉스 더 플렌더(mini) | 자사몰 → officialWebsite |
| GFA-피드 → gfa · feed | 트래픽 → traffic | 더슬림 → theslim · 미닉스 더 슬림_더 슬림 | CJ → cj |
| 인플루언서-인스타 → influencer-ig · affiliate | … | … | … |
| 인플루언서-유튜브 → influencer-youtube · affiliate | … | … | … |

> **매체 · 목적 · 상품이 늘면 빠진 줄만 자동으로 덧붙습니다.** 고쳐 둔 값은 건드리지 않습니다.
> `utm_medium` 에 지면을 적어 두면(`feed` · `shopping` · `smart` · `bizboard` …)
> 쇼핑라이브의 `sn` 도 같은 값을 씁니다. 별도 칸이 필요 없습니다.
> `마케팅팀` 은 대응하는 매체가 없어 비워 뒀습니다(그 줄은 UTM 이 만들어지지 않습니다).

### 시작하기 (한 번만)

1. `Code.gs` 를 다시 붙여넣고 저장
2. 시트를 새로고침하면 상단에 **UTM** 메뉴가 생깁니다 → **전체 행 다시 계산**
   - 이미 쌓여 있던 줄까지 UTM 이 채워집니다
   - 열 · 열 차례 · 설정 탭도 이때 맞춰집니다
3. 앱의 **적재확인** 버튼까지 새 코드로 돌리려면 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포**

### 그 뒤로는

- 앱에서 **최종완료 → 적재** 하면 그 줄에 UTM 이 함께 들어갑니다
- 시트에 **직접 타이핑하거나 붙여넣어도** 같은 UTM 이 붙습니다
- **`(세로)` 파일명은 UTM 을 만들지 않습니다** — 가로와 같은 소재라 UTM 도 가로 것을 씁니다 (앱도 적재에서 뺍니다)
- 파일명을 지우면 그 줄 UTM 도 지워집니다
- UTM 칸을 손으로 고쳐 두면 그대로 둡니다 (앞쪽 칸을 다시 건드리면 수식으로 되돌아갑니다)


## 매체별 성과 (메타 광고 API)

워크스페이스 **퍼포먼스 → 매체별 성과** 화면이 이 스크립트를 통해 메타 광고 성과를 읽습니다.
브라우저는 시트에 붙은 이 스크립트에만 물어보고, **액세스 토큰은 스크립트 안에만 있습니다.**

```
브라우저 ──▶ Apps Script(토큰 보관) ──▶ 메타 Graph API
```

### 토큰 넣기 (한 번만)

1. Apps Script 편집기 왼쪽 **프로젝트 설정(톱니)** → 맨 아래 **스크립트 속성** → **속성 추가**
2. 속성 `META_ACCESS_TOKEN`, 값에 메타 액세스 토큰 (이 저장소 `.env` 의 `META_ACCESS_TOKEN` 과 같은 값)
3. **저장**
4. 편집기 위쪽 **실행할 함수** 목록에서 `checkMetaToken` 을 골라 **▷ 실행**
   - 처음 한 번은 권한을 묻습니다: **권한 검토 → 계정 선택 → 고급 → (프로젝트 이름)(으)로 이동 → 허용**
   - 아래 **실행 로그** 에 `연결됐습니다. 광고 계정 N개` 와 계정 목록이 찍히면 된 것입니다
   - 토큰이 틀리면 그 자리에 이유가 찍힙니다 (`Invalid OAuth access token` 등)
   - 시트를 새로고침하면 상단 **UTM** 메뉴에도 `메타 연결 확인` 이 생깁니다 (같은 함수를 알림창으로 보여 줍니다)
   - `metaAccounts_` 처럼 `_` 로 끝나는 함수는 실행 목록에 뜨지 않습니다. 그래서 이 함수를 따로 뒀습니다
5. **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포** — 주소는 그대로입니다

> 토큰은 만료됩니다. 화면에 `Invalid OAuth access token` 이 뜨면 같은 자리에 새 토큰을 넣고 새 버전으로 배포하면 됩니다.
> 스크립트 속성은 이 스크립트를 편집할 수 있는 사람만 볼 수 있고, 웹 앱 응답에는 들어가지 않습니다.

### 앱이 보내는 요청

적재와 같은 주소(`/exec`)로 보내되, `action` 이 있으면 시트를 건드리지 않고 조회만 합니다.

| action | 보내는 것 | 받는 것 |
|---|---|---|
| `metaAccounts` | — | 토큰으로 볼 수 있는 광고 계정 목록 (이름 · 번호 · 통화) |
| `metaReport` | `account` · `since` · `until` · `refresh` | 캠페인 · 광고그룹별 성과와 지금 켜져 있는지 |
| `metaCreatives` · `googleCreatives` | 위에 더해 `campaign` · `adset` | 그 자리의 소재(광고)별 성과와 미리보기 주소 |

### 숫자를 만드는 규칙

- **결과** = 구매 + 장바구니 + 리드. 한 묶음에서는 먼저 잡히는 행동 하나만 셉니다
  (`omni_purchase` 가 있으면 `offsite_conversion.fb_pixel_purchase` 는 세지 않습니다 — 겹쳐 세지 않으려고)
- **CPC · CTR** 은 링크 클릭(`inline_link_clicks`) 기준입니다
- **CPA** 는 트래픽 목적(`OUTCOME_TRAFFIC` · `LINK_CLICKS`)을 뺀 광고비 ÷ 결과입니다 (계산은 화면에서 합니다)
- **게재 목록**은 지금 켜져 있는 것(`effective_status = ACTIVE`)만 올립니다.
  기간 안에 돌았지만 지금 꺼진 캠페인도 **광고비 합계에는 들어갑니다** (표 아래에 따로 적어 둡니다)
- 같은 계정 · 같은 기간은 **5분 동안 담아 뒀다가** 그대로 줍니다. **메타에서 새로 받기** 버튼은 이 캐시를 건너뜁니다


### 구글 광고도 같은 자리에서

**매체별 성과** 화면의 **Google Ads** 탭이 씁니다. 메타와 같은 구조 — 스크립트가 자격 증명을 들고 대신 부릅니다.
리프레시 토큰으로 액세스 토큰을 받아 Google Ads API(`v22`)에 GAQL 로 물어봅니다.

**스크립트 속성 5개** (값은 이 저장소 [env.txt](env.txt) 의 같은 이름 항목과 같습니다)

| 속성 | 무엇 |
|---|---|
| `GOOGLE_ADS_DEVELOPER_TOKEN` | 개발자 토큰 |
| `GOOGLE_ADS_CLIENT_ID` · `GOOGLE_ADS_CLIENT_SECRET` | OAuth 클라이언트 |
| `GOOGLE_ADS_REFRESH_TOKEN` | 리프레시 토큰 (`1//` 로 시작) |
| `GOOGLE_ADS_LOGIN_CUSTOMER_ID` | MCC 번호 (하이픈 있어도 됩니다) |

> 이름은 **접두사를 떼고 넣어도 됩니다** — `CLIENT_ID` · `CLIENT_SECRET` · `REFRESH_TOKEN` · `LOGIN_CUSTOMER_ID` 로 적어 둬도 찾아 씁니다.
> 잘못 적었을 때는 **구글 연결 확인** 이 어떤 속성이 들어와 있는지(이름과 길이만) 함께 보여 줍니다.

넣고 나서 **UTM → 구글 연결 확인** 을 누르면 계정 목록이 뜹니다. 웹 앱에도 반영하려면
**배포 → 배포 관리 → 연필 → 버전: 새 버전 → 배포** 를 한 번 하면 됩니다.

**숫자를 만드는 규칙 (메타와 다른 곳)**

- **결과** = 전환 카테고리 `PURCHASE` + `ADD_TO_CART` + `SUBMIT_LEAD_FORM`.
  카테고리가 하나도 안 잡히는 계정은 전환수(`metrics.conversions`)를 그대로 씁니다
- **CPC · CTR** 은 클릭(`metrics.clicks`) 기준입니다. 구글의 클릭은 링크 클릭이라 메타의 링크 클릭 자리에 그대로 넣습니다
- **CPA** 는 **동영상(브랜딩) 캠페인을 뺀** 광고비 ÷ 결과입니다.
  구글에는 캠페인 목적이 없어 유형(`advertising_channel_type`)으로 가릅니다 — 메타의 트래픽 목적에 해당하는 자리입니다
- 광고비 · 예산은 마이크로 단위로 오므로 1,000,000 으로 나눠 원으로 바꿉니다
- 계정 목록은 MCC 아래 `customer_client` 에서 읽고 **관리자 계정은 뺍니다**
- 액세스 토큰은 50분, 계정 목록은 10분, 성과는 5분 담아 둡니다


### 카카오모먼트도 같은 자리에서

**매체별 성과** 화면의 **카카오모먼트** 탭이 씁니다. 메타 · 구글과 같은 구조입니다.

**스크립트 속성 1개**

| 속성 | 무엇 |
|---|---|
| `KAKAO_BUSINESS_TOKEN` | 카카오 비즈니스 토큰 (`kakao_business_token.txt` 의 값) |

> **어드민 키로는 부를 수 없습니다.** 카카오모먼트는 '비즈니스 토큰' 만 받습니다.
> 비즈니스 토큰에는 리프레시가 없어 한 번 받아 두고 계속 씁니다. 자주 재발급하면 막힙니다.
> 다시 받아야 할 때는 `python get_kakao_business_token.py --redirect-uri <등록한 URI>` 를 씁니다.
> 받으려면 앱이 **비즈 앱 + 카카오모먼트 권한(심사)** 상태여야 하고,
> **REST API 키에 클라이언트 시크릿이 켜져** 있어야 합니다. 리다이렉트 URI 는
> `앱 설정 → 플랫폼 키 → REST API 키 → 리다이렉트 URI` 에 등록합니다(카카오 로그인 화면이 아닙니다).

넣고 나서 **UTM → 카카오 연결 확인** 을 누르면 광고계정 목록이 뜹니다.

**숫자를 만드는 규칙 (메타와 다른 곳)**

- **결과** = 픽셀&SDK 전환의 구매 + 장바구니 + 가입입니다.
  전환 지표 이름이 계정마다 달라서, 아는 이름을 먼저 찾고 못 찾으면 뜻이 담긴 낱말로 찾습니다.
  **카카오 연결 확인** 이 어제 보고서에 실제로 담겨 온 지표 이름을 모두 보여 주므로,
  결과가 0 으로만 나오면 그 목록을 보고 `KAKAO_RESULT_FIELDS` 를 고치면 됩니다
- **CPC · CTR** 은 클릭(`click`) 기준입니다. 카카오의 클릭은 한 가지라 메타의 링크 클릭 자리에 그대로 넣습니다
- **CPA** 는 **도달 · 동영상 캠페인을 뺀** 광고비 ÷ 결과입니다. 캠페인 유형(`campaignType`)으로 가릅니다
- 보고서에는 **id 만** 담겨 옵니다. 이름 · 유형 · 예산은 목록 API 로 따로 받아 이어 붙입니다
- **기간은 한 번에 31일까지** 입니다. 더 긴 기간을 고르면 그렇게 알려 줍니다
- 호출 제한이 빡빡합니다(보고서는 앱 기준 5초당 1회). 호출 사이를 띄우고, 막히면 한 번 쉬었다 다시 묻습니다.
  광고그룹까지 파고드는 캠페인은 **광고비가 큰 20개** 까지입니다
- 당일 수치는 **다음날 오전 8시까지 계속 바뀝니다**


## 참고

- **액세스 권한은 반드시 "모든 사용자"** 여야 합니다. "athomecorp.com 내 사용자" 로 두면 웹 앱이 로그인 페이지로 넘겨서 브라우저에서 보내는 요청이 막힙니다(적재 실패).
  - 확인: 웹 앱 URL을 브라우저 주소창에 그냥 붙여넣어 봅니다. `{"ok":true,...}` 가 보이면 정상이고, 로그인 화면이 뜨면 권한을 바꿔야 합니다.
  - 바꾼 뒤에는 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포** 를 해야 반영됩니다.
- 적재가 실패하면 **파일명만 복사** 로 시트에 붙여넣을 수 있습니다. (매체 · 최종행사명은 따라가지 않습니다)
- URL은 브라우저에 저장되므로 **사용하는 사람마다 한 번씩** 넣어야 합니다.
- 연동을 끄려면 톱니바퀴에서 URL을 비우고 확인하세요.
- 코드를 고친 뒤에는 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포**를 해야 반영됩니다.
